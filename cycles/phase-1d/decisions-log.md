---
document_type: decisions-log-archive
level: ops
version: "1.0"
status: archive
producer: state-manager
cycle: phase-1d
inputs: [STATE.md]
input-hash: "[live-state]"
traces_to: STATE.md
---

# Superseded Decisions — phase-1d

<!-- Decisions that were explicitly superseded and archived from STATE.md.
     Active standing decisions remain in STATE.md Decisions Log. -->

| ID | Decision | Rationale | Phase | Date | Made By |
|----|----------|-----------|-------|------|---------|
| D-002 | Run pipeline LOCAL-ONLY, no GitHub remote / no PRs | Repo has no `origin` remote; human elected local-only delivery | pre-1 | 2026-08-05 | **SUPERSEDED by D-021** |

## Full Decisions Log D-001..D-213 — extracted from STATE.md 2026-08-10 (Burst-42 compaction)

Complete row set as it stood in STATE.md at the Burst-41 commit. D-002 appears
only in the Superseded Decisions section above. Load-bearing standing decisions
are ALSO retained inline in STATE.md; this section is the exhaustive record.

| ID | Decision | Rationale | Phase | Date | Made By |
|----|----------|-----------|-------|------|---------|
| D-001 | Initialize .factory worktree via /vsdd-factory:factory-health | factory-artifacts branch and worktree were missing; pre-existing logs/ and sidecar-learning.md preserved | pre-1 | 2026-08-05 | human/orchestrator |
| D-003 | Phase 3 autonomy = run to convergence, stop only at designed human gates | Human decision; no per-story or per-wave pauses | pre-1 | 2026-08-05 | human |
| D-004 | Target language Rust, MSRV 1.85, toolchain pinned to 1.97.0 | BRIEF.md specifies Rust; MSRV 1.85 forced by clap 4.6 + ureq 3.3 per market-intelligence.md | pre-1 | 2026-08-05 | dx-engineer/orchestrator |
| D-005 | Copy BRIEF.md to .factory/specs/product-brief.md with canonical VSDD L1 frontmatter; keep root BRIEF.md as frozen original | artifact-detection.md flagged path discrepancy; BRIEF.md is frozen and must not be edited | pre-1 | 2026-08-05 | orchestrator |
| D-006 | Strict case-sensitive + NFC path comparison on all platforms (macOS, Linux, Windows) | Brief's determinism premise; macOS APFS is case-insensitive and NFD — native semantics would classify the same repo differently locally vs in CI | phase-1 | 2026-08-05 | human |
| D-007 | Narrow HTML `id=`/`name=` anchor carve-out; no DOM, no HTML link following | Literal "no HTML parsing" non-goal would make every `<a name>`-defined anchor a false positive | phase-1 | 2026-08-05 | human |
| D-008 | Three link verdicts: clean / broken / indeterminate; indeterminate never causes nonzero exit | Real servers return 429/403/5xx to CI runners; strict classification would produce the CI false positives the brief targets | phase-1 | 2026-08-05 | human |
| D-009 | IN scope: factory-authored README.md, acceptance corpus. OUT: inline suppression directives, GFM bare-URL autolinks | Brief references an acceptance corpus and README that did not exist; suppression and bare autolinks were scope creep | phase-1 | 2026-08-05 | human |
| D-010 | The `mdlinkcheck BRIEF.md` → exit 0 vector is VISIBLE, not a holdout; replacement holdouts designated | It is the canonical R4 proof stated in the brief itself, so it cannot meaningfully be hidden | phase-1d | 2026-08-05 | human |
| D-011 | `--quiet`, `--offline`, `--insecure`, `--hidden` DROPPED as explicit non-goals | No BC coverage and not in R1–R8; `--offline` is redundant since offline is the default | phase-1d | 2026-08-05 | human |
| D-012 | Discovery matches `.md` only, case-sensitive; `.MD`, `.markdown`, `.mdx` are non-goals | Literal reading of R1 and consistent with D-006 strictness | phase-1d | 2026-08-05 | human |
| D-013 | NFR-001 (5s p95 Apple Silicon) / NFR-002 (15s p95 Linux CI) confirmed as acceptance ceilings from frozen R8, PLUS NFR-008 ~500ms p95 Tier A regression gate | The R8 ceiling is ~25–100x looser than realistic runtime; it cannot detect regression without the tighter NFR-008 gate | phase-1d | 2026-08-05 | human |
| D-014 | Verdict vocabulary is TWO LAYERS: link verdict = clean/broken/indeterminate (closed, drives exit codes); URL liveness outcome = alive/broken/indeterminate (intermediate, --online only). `alive` is NOT a fourth verdict | error-taxonomy.md had flattened the layers and wrongly asserted "clean is NOT used for external URL verdicts", contradicting DI-005 | phase-1d | 2026-08-05 | orchestrator ruling (DD-022) |
| D-015 | Error taxonomy canonical ruling: 13-code closed set is authoritative; exit 1 for broken links per frozen R7; DNS failure and TLS error are `broken` not `indeterminate` | ADR-007 had inverted the exit codes, invented 5 phantom reason codes, and misclassified dns/tls | phase-1d | 2026-08-05 | orchestrator ruling |
| D-016 | Add optional `sub_reason` field to the JSON finding object rather than stripping sub-reason diagnostics | `private-ip` and `https-downgrade` sub-reasons exist in BC test vectors with no schema field to carry them; additive and pre-1.0 | phase-1d | 2026-08-05 | orchestrator ruling |
| D-017 | JSON output is an object envelope `{schema_version, results[], errors[]}`, superseding R6's "may be formatted as an array" wording; consumers MUST read `.results`, not treat the top-level value as an array; `schema_version` field enables schema-change detection [RECONSTRUCTED from prd.md:685-686 and prd.md:311-312 — original STATE.md row lost] | R6 frozen brief states output "may be formatted as an array"; object envelope accommodates the `errors[]` array and schema versioning; full contract in BC-2.13.001 and interface-definitions.md §6 | phase-1d | 2026-08-05 | human |
| D-018 | HTTP 400-after-GET fallback is classified as `indeterminate` (not `broken`), yielding exit 0; fixed BC-2.10.002 PC3 and PC3b and error-taxonomy.md `http-error` trigger note [RECONSTRUCTED from prd.md:688-689 — original STATE.md row lost] | ADR-007 (accepted) misclassified 400-after-GET as `broken`/exit 1, contradicting all three taxonomy sources (error-taxonomy.md, failure-modes.md, prd.md §2 HTTP ruling); 400-after-GET is an ambiguous server response that does not definitively indicate the resource is broken | phase-1d | 2026-08-05 | human |
| D-019 | `--allow` matching algorithm: attempt WHATWG normalization first; if normalization fails (malformed URL), fall back to raw-string prefix match at a component boundary (next char after prefix must be `/`, `?`, `#`, or end-of-string); boundary rule applies in both paths; rewrote BC-2.11.002 and interface-definitions.md §2.2 [RECONSTRUCTED from prd.md:691-692 and BC-2.11.002.md:23-24 — original STATE.md row lost] | Malformed intranet URLs (e.g., `https://build_server/status`) that WHATWG cannot parse are the primary `--allow` use case; a naive `starts_with` without boundary check would permit `https://example.com.evil.tld` to bypass the prefix (P2-M08) | phase-1d | 2026-08-05 | human |
| D-020 | EC-036, EC-049, EC-074, EC-157, EC-158 burned from holdout designation to visible tests; four fresh hidden replacement scenarios authored (EC-165..EC-168: anchor resolution, source-exclusion × cross-file anchors, percent-encoding × fragment split, duplicate-slug collisions); HS-INDEX.md updated (HS-004..HS-007 active; HS-002/003 retired) [RECONSTRUCTED from prd.md:694-695 and adversary-pass-3.md:186-190 — original STATE.md row lost] | Five holdout ECs had leaked into visible specs, making them unsuitable as hidden scenarios; burning them restores holdout pool integrity and maintains Phase 4 evaluation blind spot; P3-005 finding confirmed the burn was mandated but never executed in test-vectors.md | phase-1d | 2026-08-05 | human |
| D-021 *(clause "agents NEVER merge" partially superseded by D-028)* | Full PR-based delivery restored; merge gate = required CI status checks + factory AI review (pr-reviewer / code-reviewer); ~~agents NEVER merge PRs — orchestrator merges only after human approval at a designed gate~~ *(superseded: agents MAY merge after full pr-manager review lifecycle per D-028)* | devops-engineer agent attempted to self-merge PR #1 and was blocked by the security guard; `required_approving_review_count` is 0 on both branches because every agent-authored PR is authored by the human's own GitHub account (`drbothen`) and GitHub forbids self-approval; remainder of D-021 strengthened by D-028 | phase-1d | 2026-08-05 | human |
| D-022 | Branch topology: story PRs target `develop`; releases go `develop` → `main` via PR. Branch protection live on both with 8 strict required status checks, linear history, no force-push/deletion; `enforce_admins` true on `main` | Establishes the two-branch delivery model replacing the D-002 local-only workaround | phase-1d | 2026-08-05 | devops-engineer/human |
| D-023 | CI jobs use `Cargo.toml`-presence guards so they pass green before the Rust workspace exists; required status check context strings are the unprefixed job names verified empirically from live run 31065845669, NOT the guessed `CI / `-prefixed form | Avoids branch-protection deadlock on bootstrap PR; context string form verified from live CI run | phase-1d | 2026-08-05 | devops-engineer |
| D-024 | Nonexistent/unreadable PATH argument is a runtime I/O error (recorded, scan continues, exit 2 at end) — NOT a startup config error with immediate abort. Required by DD-007 no-fail-fast | architect ruling; PO reconciled | phase-1d | 2026-08-05 | architect |
| D-025 | `verdict::exit_code(findings, io_errors, config_error) -> u8` — three inputs. Precedence: io_errors non-empty OR config_error → 2; elif any broken → 1; else 0; indeterminate never raises the exit code | P2-M19 found the config_error half of exit 2 unmodeled; three-input function closes that gap | phase-1d | 2026-08-05 | architect |
| D-026 | BC `## Edge Cases` tables now cite EC registry IDs plus a one-line label ONLY; all concrete inputs and expected verdicts live solely in `prd-supplements/test-vectors.md` as the single canonical EC registry (POL-16) | Duplicating expectations in both BCs and the registry was the structural cause of 98 EC-ID collisions, six with opposite expected verdicts. Makes the collision class unrepeatable rather than merely repaired | phase-1d | 2026-08-06 | product-owner/orchestrator |
| D-027 | Validator hardening is mandatory before trusting any validator result | `check-id-resolution` initially returned PASS while never checking EC IDs; `check-counts` missing 3 checks; `check-placeholders` had 23 false positives + missed 55 real defects. All 8 checkers now have negative tests proving they can fail | phase-1d | 2026-08-06 | devops-engineer/orchestrator |
| D-028 | Merge policy REVISED — agents MAY merge PRs, but ONLY after the full vsdd-factory PR review process. Direct unreviewed merges remain FORBIDDEN. SUPERSEDES the "agents NEVER merge" clause of D-021. | Operator directive. D-028 unlocks agent-merge post-review to remove human-blocking of routine story PRs. | phase-1d | 2026-08-06 | human/operator |
| D-029 | `spec-lint` CI job remains ADVISORY (NOT in required status checks) until adversary pass 4 demonstrates convergence; flip to REQUIRED at Phase 1 approval | Operator directive. 25 known `[filled by story-writer]` violations legitimately outstanding until Phase 2; making spec-lint blocking now would deadlock PRs for a non-defect | phase-1d | 2026-08-06 | human/operator |
| D-030 | Session wrap — durable RESUME SNAPSHOT D-030 committed to factory-artifacts | Zero-context resume; single-commit burst TD-VSDD-053; wrap at end of session before context clear | phase-1d | 2026-08-06 | state-manager |
| D-031 | Merge autonomy set to level 4 — agents merge PRs themselves with no human merge gate, ONLY after the full pr-manager review lifecycle passes. Recorded in `.factory/merge-config.yaml`. Direct unreviewed merges remain FORBIDDEN. | Operator directive to remove human blocking on routine merges while preserving full review rigor. | phase-1d | 2026-08-06 | human/operator |
| D-032 | `spec-lint` CI job remains ADVISORY through adversary pass 4; flip to REQUIRED status check at the Phase 1 human approval gate. Reaffirms and time-boxes D-029. | Operator directive. | phase-1d | 2026-08-06 | human/operator |
| D-033 | BI-005 closed at spec level; residual implementation risk re-scoped to new BI-007 blocking phase-6, not phase-1. Closing a spec gap and discharging an implementation obligation are recorded as SEPARATE events. | Collapsing the two events is precisely how FM-002 became invisible. | phase-1d | 2026-08-06 | orchestrator |
| D-034 | `prd.md` versioned changelog entries are IMMUTABLE audit records and MUST NOT be retroactively updated. Ruling applies generally to all `prd.md` changelog sections. | Editing them would falsify the audit trail rather than fix a defect. | phase-1d | 2026-08-06 | orchestrator |
| D-035 | Adversary pass 4 recorded 37 findings with ZERO in the 7 mechanically-enforced classes. Enforcement judged SUCCESSFUL on composition, INEFFECTIVE on magnitude. Root cause reclassified from spec-quality to SPEC-TOPOLOGY. | ~15 documents hand-maintain restatements of the same facts with no generated source of truth. | phase-1d | 2026-08-06 | orchestrator |
| D-036 | Pass 5 will NOT run against the current spec topology. Remediation sequence: (1) adjudicate BI-009, (2) close BI-008/BI-011, (3) land BI-012 generators + canonical-facts block, (4) THEN run pass 5. | Running pass 5 against unchanged topology would re-sample the same causes and produce another plateau reading. | phase-1d | 2026-08-06 | orchestrator |
| D-037 | Adversary agent tool-profile gap: the `adversary` agent is read-only (Read/Grep/Glob) and CANNOT write its own report, so pass-4's findings required orchestrator-side recovery. Factory process-gap. | Near-loss of the single most valuable artifact of the session. | phase-1d | 2026-08-06 | orchestrator |
| D-038 | PR #2 review-cycle limit exceeded with recorded exception: 7 cycles. Merge permitted because all blocking findings resolved and merging tooling with known false-passing tests was strictly worse. | Operator directive selected "fix the pattern, not the instances." | phase-1d | 2026-08-06 | human/operator |
| D-039 | Remediation of a defect class MUST NOT be achieved by suppressing detection. Allowlists, skip-lists, deferral sets, and known-issues collections are FORBIDDEN in any spec-lint checker, enforced by a pre-flight structural guard. | Blinding a checker is worse than editing a spec to suit it: it is durable, silent, and survives every future run. | phase-1d | 2026-08-06 | orchestrator |
| D-040 | A validator earns a place on an adversarial-review SKIP LIST only once it has a negative test proving it CAN fail. Pass 5's skip list must be re-derived from proven-can-fail evidence, NOT from the pass-4 list. | Pass 4's skip list was partly built on `check-ec-injectivity`, which could not fail. | phase-1d | 2026-08-06 | orchestrator |
| D-041 | Orchestrator sequencing error: a devops burst was dispatched onto a branch another burst held merge-and-delete authority over. Factory process-gap. | LESSON: never dispatch a burst onto a branch another burst is authorized to merge or delete. | phase-1d | 2026-08-06 | orchestrator |
| D-042 | Adversary pass 4 and consistency pass 4 INDEPENDENTLY converged on the same defect (P4-007 == C4-001/C4-002). Independent convergence from different methods is the strongest available evidence a finding is real. | Two fresh contexts, different mandates, same conclusion. | phase-1d | 2026-08-06 | orchestrator |
| D-043 | Platform matrix narrowed to **macOS latest ONLY**. Linux and Windows dropped from CI test/build jobs (8 → 4 branch-protection contexts). NFR-002 RE-TARGETED to 10s p95 macos-latest. NFR-004 RETIRED. T13 retired. **D-006 / ADR-006 / DI-002 REMAIN IN FORCE**, rationale restated on determinism grounds. `unicode-normalization` pinned 0.1.24, load-bearing for VP-008/VP-009. | Operator directive; simplifies CI (9 → 5 jobs per run, −44%). | phase-1d | 2026-08-06 | human/operator |
| D-044 | Platform-independent CI jobs remain on `ubuntu-latest` under D-043. Only genuinely macOS-dependent jobs (`Test`, `Build release`) moved to `macos-latest`. | macOS runners cost ~10x Linux; cargo fmt/clippy/spec-lint/GitGuardian have no OS-specific behaviour. | phase-1d | 2026-08-06 | orchestrator |
| D-045 | Dropping Linux/Windows CI removes an INCIDENTAL safety net for case-sensitivity and Unicode-normalization divergence. VP-008/VP-009 solely load-bearing. BC-2.07.003 formal hardening priority RAISED. | Independent convergence from architect and product-owner bursts (D-042 principle). ADR-006 now MORE load-bearing on macOS-only matrix. | phase-1d | 2026-08-06 | orchestrator |
| D-046 | Restricted-path merge gate WAIVED by operator for PR #3 and PR #4 ONLY. `merge-config.yaml` NOT amended — rule remains in force for all future PRs. | Both PRs were complete before the waiver need was identified. | phase-1d | 2026-08-06 | human/operator |
| D-047 | Merge wrapper scripts built at `.factory/bin/` rather than `plugins/vsdd-factory/bin/`. | `plugins/` belongs to the engine and MUST NOT be created in a product repo. `.factory/bin/` is the correct location. | phase-1d | 2026-08-06 | operator/orchestrator |
| D-048 | INC-MAP-001 recorded as `SPEC-RESOLVED / IMPL-PENDING`, NOT `RESOLVED`. `bc-module-map.md` v1.4. | Per D-033, closing a spec gap and discharging an implementation obligation are separate events. | phase-1d | 2026-08-06 | orchestrator |
| D-049 | Review cycle 5 EXCEPTION granted for PR #3 by operator; full mutation audit of ALL 15 selftests ordered. | B-4 and B-6 found independently — over-determination may be systemic. | phase-1d | 2026-08-06 | human/operator |
| D-050 | Full mutation audit found 4 of 15 selftests over-determined (27%). Green negative-test suite is NOT evidence of trustworthiness without mutation verification. Standing rule: mutation verification required for all future skip-list entries. | Three of four would have shipped invisibly if only B-6 was patched. | phase-1d | 2026-08-06 | human/operator |
| D-051 | Pass-5 gate interpretation: D-036 satisfied by COMMITTED-AND-VERIFIED spec artifacts on `factory-artifacts`, NOT merge-to-develop. | Pass 5 reviews spec artifacts on factory-artifacts; outage blocking merge does not block spec-artifact review. | phase-1d | 2026-08-06 | human/operator |
| D-052 | Restricted-path waiver extended to PR #5 (`.github/**`). Scoped to PR #5 only; `merge-config.yaml` NOT amended. | Same restricted-path class as D-046. Operator explicitly approved. | phase-1d | 2026-08-06 | human/operator |
| D-053 | `cargo-mutants` belongs on `macos-latest` under D-043 — mutation testing is platform-DEPENDENT; D-044's ubuntu-for-cost rule applies only to genuinely platform-independent jobs. Applied in PR #5 hardening.yml. | macOS-only matrix means a mutation breaking case-insensitivity handling would survive on ext4. | phase-1d | 2026-08-06 | human/operator |
| D-054 | SS-10 `--online` descope DENIED. `--online` is a BRIEFED SURFACE; removing it requires a brief change, not a pipeline scope decision. Closes the descope option; do not re-propose. | `--online` is in the brief; brief changes require human authorization. Difficulty is not grounds for a brief change. | phase-1d | 2026-08-07 | human/operator |
| D-055 | D-055 CORRECTED: run 31122163633 was on superseded head `af54a65`, not `6503d3b`. PR #4 had ZERO CI runs at real head. `workflow_dispatch` runs are excluded from PR status-check rollup — only a `pull_request`-typed run satisfies branch protection. | Cancellation artifacts confirmed (code is green). `workflow_dispatch` invisible to PR rollup is a standing environmental finding. | phase-1d | 2026-08-07 | human/operator |
| D-056 | Stash list EMPTY confirmed in all four worktrees. "Needs user-space git stash drop" caveat RESOLVED. | Operator verified stash lists directly. | phase-1d | 2026-08-07 | human/operator |
| D-057 | CORRECTION TO D-050: mutation verification is NECESSARY BUT NOT SUFFICIENT. Positive-coverage counts (runtime-computed "N validated, 0 non-conforming") is the durable admission criterion for skip-list entries. Evidence: BI-023. | Mutation tests only exercise the test author's known-bad input set, not unknown shapes. BI-023 is the existence proof. | phase-1d | 2026-08-07 | human/operator |
| D-058 | `covered_sha` hand-editing REJECTED as a merge-authorization mechanism. VINDICATED: the review the snapshot proposed to skip found two BLOCKING defects (B-7, B-8). | Editing the record to satisfy `check-stale-verdict.sh` defeats the control. | phase-1d | 2026-08-07 | human/operator |
| D-059 | `strict: true` re-staleness policy: a merge-only SHA change MAY retain its verdict ONLY with a MECHANICAL EQUIVALENCE PROOF that the PR's own patch is byte-identical before and after the update. First applied to PR #5. | Distinguishes pure merge-commit from content change. | phase-1d | 2026-08-07 | human/operator |
| D-060 | Pass-6 skip list: re-audit ALL THREE remaining entries (`check-counts`, `check-adr-consistency`, `check-title-sync`) for positive-coverage soundness. They were admitted on the same mutation-only D-050 evidence that B-8 disproved. Resolves PENDING OPERATOR DECISION (b). | D-050 mutation-only evidence is not sufficient per D-057. | phase-1d | 2026-08-07 | human/operator |
| D-061 | DirIndex scope: BROAD — `purity-boundary-map.md` is AUTHORITATIVE. Every extracted link destination populates DirIndex. The narrow reading was architecturally cornered: `Path::exists()` violates DI-002; I/O in `path_resolver` violates the pure-core boundary. Operator ruling. | DI-009 termination bound re-derived as `O(unique parent dirs of all link destinations)`. BC-2.07.002/003/005/006 and DI-009 updated atomically. | phase-1d | 2026-08-07 | human/operator |
| D-062 | `config_error` canonical list = {invalid `--ignore` glob} ONLY, routed THROUGH `verdict::exit_code` with `config_error=true`. The `process::exit(2)` bypass REJECTED. Closes BI-030. | SS-14 exit-code domain ambiguity resolved. CAP-014 corrected. `--help`/`--version` in cli.rs outside the lattice. | phase-1d | 2026-08-07 | human/operator |
| D-063 | SS-10 mechanical corrections approved: CAP-014 label fixed; BC-2.14.004 Invariant 4 added; BC-2.14.002 Precondition 3 removed (its own test vector contradicted Precondition 3). | Six artifacts agreed; CAP-014 was the outlier. Self-contradiction in BC-2.14.002 closed. | phase-1d | 2026-08-07 | human/operator |
| D-064 | Cycle-limit exception granted for the PR #3 review at `031ca5b` (`max_review_cycles: 3`; PR #3 near cycle 8). Extends D-049/D-038 lineage. | Four consecutive failures all in `check-index-integrity.py`; design reassessment may resolve without additional review cycles. | phase-1d | 2026-08-07 | human/operator |
| D-065 | D-040 SCOPE CLARIFICATION: D-040 is SATISFIED when a defect is detected by a stronger subsuming backstop even if the narrower mutation no longer flips a test — detection is what D-040 protects. Evidence: MUT-1 caught by accounting invariant; MUT-3 still flips test 10e. Extends D-050→D-057 lineage. | Mutation no longer flipping a test is acceptable if a stronger backstop catches the same defect class. | phase-1d | 2026-08-07 | human/operator |
| D-066 | PR #3 patch-fixing HALTED; DESIGN REASSESSMENT ordered. Four consecutive failures all in `check-index-integrity.py`, each fix opening a new hole of the same shape. Read `cycles/phase-1d/design-ruling-index-integrity.md`. Option C (generate from frontmatter, reusing BI-012 infrastructure) is the one to weigh hardest. Session wrap — single-commit burst TD-VSDD-053. | Four consecutive failures in one component is a design signal, not a quality signal. Option C eliminates the parse-fidelity class entirely; Phase 3 not started so structural change is cheapest now. | phase-1d | 2026-08-07 | human/operator |
| D-067 | Operator authorized PR #3 B-11 stopgap per architect design ruling. Structural fix (counter above pre-filters) deferred to two follow-on stories. D-028 full review lifecycle still required. PR #3 head `031ca5b` → `51e6be8`. Suite 21→24/24. Three KNOWN bypasses closed (A1/A2a/A2b now exit 1, mutation-verified). Narrowed pre-filters NOT structural independence. `fix/hardening-pins` local branch + worktree confirmed removed. D-066 halt superseded. | Architect ruling: land stopgap, create separate structural stories. Honest documented residual preferred over false independence claim. | phase-1d | 2026-08-06 | human/operator |
| D-068 | Operator authorized the STRUCTURAL fix to `check-index-integrity.py` REVERSING D-067's deferral of counter placement. Rationale: D-067 stopgap premise falsified by execution — orchestrator reproduced `exit 0` / `Check passed` with a phantom `HS-099 → EC-999` row present while accounting invariant reported `7 rows seen`, blind to the 8th row because `hs_rows_seen++` sat BELOW the pre-filters. Narrowing pre-filters shown unboundedly leaky. | Counter MUST move above all pre-filters; structural independence is REQUIRED, not deferred. | phase-1d | 2026-08-07 | human/operator |
| D-069 | After D-068 fix closed named shapes but a fresh review found a further BLOCKING regression, operator BANNED spelling-specific patching and required a PROPERTY-BASED restructure: count every non-blank line into a denominator BEFORE any predicate, classify into six buckets afterward, assert conservation law (`total_candidates == sum(buckets)`). Implemented at `f44147e` with 300-case seeded property test. | Spelling-specific patching is unboundedly leaky; a structural invariant with auditable bucket accounting is the only durable fix. | phase-1d | 2026-08-07 | human/operator |
| D-070 | Fresh review of `f44147e` found two BLOCKING bypasses AND the meta-defect that the conservation law constrains TOTALITY but not CORRECTNESS (`prose` and `fenced_code` are unbounded sinks; five mutations each reopen a historical bypass while property test still passes 300/300). Operator ruled SPLIT: land bounded fix now (tab-vs-4-spaces and unguarded-indented-fence bypasses closed at `6e785b4` + `7c1eccf`); carry the defect CLASS to a separate Option-3 story (BI-040). | spec-lint is ADVISORY until Phase 1 gate; PR #3 carried independent value; class is tracked with landing gates. Conservation law proves totality, not correctness — unbounded sinks are a structural design problem requiring the Option-3 story. | phase-1d | 2026-08-07 | human/operator |
| D-071 | PR #3 merged as `651ee3a` under D-028/D-031 autonomy level 4 after full pr-manager review lifecycle: APPROVE at `6e785b4`, confirmatory APPROVE at `7c1eccf` (MAJOR-1 docstring fix). Freshness re-established via `check-stale-verdict.sh 3 <7c1eccf full SHA>`; merge executed via `enforce-merge-strategy.sh 3 --squash --delete-branch`. Non-advisory CI green; `Spec lint` FAILURE accepted as advisory per D-029/D-032. | Full review lifecycle completed; all blocking findings resolved; merge gate controls verified sound (tautological invocation of check-stale-verdict caught and corrected before merge). | phase-1d | 2026-08-07 | orchestrator |
| D-072 | BI-040 must close BEFORE WS-4 begins — hard ordering. WS-4 remediates ~306 findings and uses spec-lint checkers as the verification oracle; a verifier with a known-open bypass family must not be the oracle for its own fixes. Queue: WS-2→WS-3→WS-3b (Option-3 story, BI-040 as landing gate)→WS-4→WS-5. | D-050/D-057 lesson applied to our own tooling. Running WS-4 before BI-040 closes trades a known blind spot for an approved oracle. | phase-1d | 2026-08-07 | human/operator |
| D-073 | PR #6 permitted to merge with BI-035 only HALF-discharged. FACT-7 and FACT-8 production patterns verified sound. FACT-9 and FACT-10 NOT discharged. Ordered debt: BI-042 is the FIRST work item the moment the concurrent `.factory/specs` editor releases the tree, ahead of WS-3 and WS-3b. | Merger of PR #6 carried independent value; FACT-9/FACT-10 gap tracked explicitly with mandatory ordering. | phase-1d | 2026-08-07 | human/operator |
| D-074 | PR #6 merged as `7b9aa6d` under D-028/D-031 autonomy level 4 after full review lifecycle (REQUEST_CHANGES at `a642d24`; APPROVE-with-eyes-open at `0ad5c5e` declined by orchestrator — MAJOR-4..7 sent back; REQUEST_CHANGES at `06b58b6`; APPROVE at `90840ca`). Freshness via `check-stale-verdict.sh 6 <90840ca full SHA>`; merged via `enforce-merge-strategy.sh 6 --squash --delete-branch`. Rebase rejected; cherry-pick of `78ef3a4` used (six of stale branch's seven commits already on develop as `651ee3a`). | Full review lifecycle completed; all blocking findings resolved. Reviewer independently confirmed all 36 develop selftests survived cherry-pick. | phase-1d | 2026-08-07 | orchestrator |
| D-075 | D-073 clarified as PREEMPTIVE, not serializing. BI-042 preempts the queue the MOMENT the concurrent `.factory/specs` editor releases the tree; it does NOT block checker-only work. The single HARD constraint is that BI-042 completes BEFORE WS-4 begins (D-072). | Keep-moving forbids idling on a lock we do not hold. | phase-1d | 2026-08-07 | human/operator |
| D-076 | BI-042 binding-pattern rule ADOPTED: a capture group must be a bounded wildcard, never the literal canonical value, with a reliable right-side delimiter, modeled on FACT-7/FACT-8's `(.*?)`+delimiter form. Captures must be LINE-BOUNDED (`([^\n]*?)` or negated classes, never bare `.*?`), because the checker runs `re.DOTALL` and em-dash is house style at 1,787 occurrences across 133 spec files. Scope: all 26 tautological bindings corrected (24 applied; FACT-6a/6b structural limitation, verified BENIGN — `None → DIVERGE` catches ID substitution). Selftest-22 rewrite specified at `cycles/phase-1d/bi-042-binding-adjudication.md §9.4`. Count corrected 17 → **26 of 31** tautological. | Prefix-extension false negatives; "17 of 31" undercounted by omitting the entire FACT-1 family (8 bindings) and FACT-2. `None → DIVERGE` always catches complete replacement; genuine false-negative class is narrowly PREFIX-EXTENSION. | phase-1d | 2026-08-07 | human/operator |
| D-077 | Sequencing (c)→(a): land checker detection now, accept a redder ADVISORY `spec-lint`, burn exposed rows down on a schedule that MUST clear before the Phase-1 convergence gate. | Detection must precede remediation or completeness is unverifiable; `check-placeholders` was already red on 25 items so marginal signal cost is near zero. | phase-1d | 2026-08-07 | human/operator |
| D-078 | `VP-NONE` reserved sentinel admitted ONLY alongside a non-empty Proof Method, expressed as admitted grammar, never a skip-set. | Em-dash is ambiguous between "no VP required" and "nobody filled this in"; a sentinel is a positive, greppable, reviewable assertion. | phase-1d | 2026-08-07 | human/operator |
| D-079 | BI-044 OPENED. EC-shape detection repaired now in `check-id-resolution.py`; the remaining 16 sites BLOCK WS-4. | Same oracle-integrity principle as D-072. | phase-1d | 2026-08-07 | human/operator |
| D-080 | One PR, one selftest allocation, one review lifecycle for all develop-side spec-lint integrity work. BI-042 selftest-22 rewrite and WS-3 Phase 2 items both in `fix/ws3-spec-lint-integrity`. | Both edit the single 3250-line `run-selftests.sh` and both allocate selftest numbers; concurrent PRs would reproduce the D-074 rebase/cherry-pick situation. | phase-1d | 2026-08-07 | human/operator |
| D-081 | R3-C extended to scope out non-conforming IDs beneath `### vN.N` versioned-changelog headings, by document position (a function, not a named set). | `prd.md:721` is immutable under D-034 and records already-completed remediation, making the finding unclosable; a permanently-red checker invites a skip-list later. Accepted residual: a genuine live defect authored INSIDE a versioned changelog section would be missed. | phase-1d | 2026-08-07 | human/operator |
| D-082 | PASS-6 HARD CONSTRAINT: any quantitative claim in a finding or dispatch must come from an EXECUTED predicate, not from reading or counting. | Four consecutive issue magnitudes were misfiled by enumeration-by-reading; operationalizes the verify-efficacy lesson. | phase-1d | 2026-08-07 | human/operator |
| D-083 | HOLD EC-registration (WS-3 item 1) until BI-044 settles. Do not allocate EC IDs against a counting toolchain known broken at 16 sites. | Same oracle-integrity principle as the WS-4 gate. | phase-1d | 2026-08-07 | human/operator |
| D-084 | Build a SHARED spec-lint primitive layer closing BI-040, BI-044 and BI-021/BI-043 together. Design at `cycles/phase-1d/bi-040-primitive-layer-design.md`. | All three are the same missing abstraction — 14 checkers/generators each re-derive line splitting, ID grammar and repo-root resolution independently, and each re-derivation is an independent chance to be wrong. | phase-1d | 2026-08-07 | human/operator |
| D-085 | WS-3b is a PHASE-1D REMEDIATION ITEM (design doc + fix PR), NOT a Phase-2 story artifact. `.factory/stories/` holds only `.gitkeep`, Phase 2 has not started, and authoring Phase-2 artifacts now would pollute Phase-2 decomposition; additionally a story merely existing cannot satisfy D-072, which requires BI-040 to CLOSE. | Phase 2 has not started; a story file cannot substitute for closing a blocking issue. | phase-1d | 2026-08-07 | human/operator |
| D-086 | WS-4 scope re-derivation BY EXECUTION is MANDATORY: it is the FIRST act after the BI-040 primitive layer lands and BEFORE any remediation dispatch. BI-040 was filed at 2 known members; execution measured 54+63 sites across 14 files and a 31-codepoint family — roughly 15× its filed size. WS-4's ~306-finding scope was sized by the same reading-and-counting method that produced four consecutive misfiled magnitudes. | D-082's executed-predicate constraint applied to WS-4's own sizing. | phase-1d | 2026-08-07 | human/operator |
| D-087 | Run `/vsdd-factory:compact-state` at next session start, before any new work. | STATE.md at 266+ lines against 200-line soft target; must slim to stay readable. | phase-1d | 2026-08-07 | human/operator |
| D-088 | State-manager must NOT use `git add -A` in state bursts while a PR agent is in flight; stage by explicit path. Path-scoping recorded as standing improvement item. | Burst `7e0f02a` incidentally committed another agent's in-flight `code-delivery/` artifacts. | phase-1d | 2026-08-07 | human/operator |
| D-089 | Gate #28 operator ruling: authorized review-as-comment + autonomy-L4 merge mechanism for the standing model. Grounds: (1) `develop` branch requires 0 approving reviews; real gate is 4 required status checks (`Format check`, `Clippy (deny warnings)`, `Test (macos-latest)`, `Build release (macos-latest)`), with `spec-lint` NOT among required checks; `enforce_admins: false`; no GitHub protection is circumvented; (2) standing designed-autonomy model (D-021→D-028→D-031); (3) journal 2026-08-05 22:35 precedent; (4) gate #5 as revised 2026-08-06 09:15; (5) BI-039 review-as-comment ruling. Residual: AI review independence is nominal, not structural. Pre-Phase-3 condition: BI-046 (separate reviewer identity/token) REQUIRED before story PRs begin. | Real-world GitHub constraint: `drbothen` authors all PRs; self-approval is prohibited by GitHub; review-as-comment is the only viable mechanism. The 4 required CI status checks constitute the real gate. BI-046 filed to resolve the structural residual before Phase 3. | phase-1d | 2026-08-07 | human/operator |
| D-090 | Session wrap — durable RESUME SNAPSHOT D-090 committed to factory-artifacts | Zero-context resume capability; single-commit burst TD-VSDD-053; wrap triggered at end of session before context clear | phase-1d | 2026-08-07 | state-manager |
| D-091 | WS-4-G Shard-A burst wrap — BC-2.05.003 v1.3 citation-authority repair committed to factory-artifacts. Single-commit burst TD-VSDD-053. POLICY-5 MEANING-INVERTED CLOSED; WS-4 scope 177 items (138 mechanical + 39 POLICY-5 FABRICATED). Prescription said "3 lines, DD-007→DI-007+DD-003"; execution found 4 lines and three distinct IDs (DD-003, D-007, DI-007) plus unprescribed wrong R5→R2b. Pass-7 intake item logged: DD-007 gloss discrepancy unadjudicated. .factory/hooks/verify-sha-currency.sh STILL ABSENT — post-push verification gap. | Un-executed prescriptions are upper bounds AND can be qualitatively wrong, not merely overstated in magnitude. CORRECTIONS REGISTER appended to SESSION-HANDOFF.md §D-090. | phase-1d | 2026-08-07 | state-manager |
| D-092 | Extend POL-14's conforming-value grammar to accept an exact `test-sufficient` sentinel, conditional on a cross-check against VP-INDEX's BC classification for that BC. Unconditional acceptance would be an allowlist (D-039); the VP-INDEX join is what makes it a genuine consistency rule. Requires new mutation/negative tests per D-040/D-050/D-057 admission criterion. **LANDED ON DEVELOP as d4e76fa (PR #9 merged).** | Operator ruling this session. All 55 em-dash rows are test-sufficient BCs; writing VP ids there would be fabrication. BC-2.10.002:160-161 correctly REJECTED by D-092's cross-check (VP-007 is assigned; misfiled rows). | phase-1d | 2026-08-07 | human/operator |
| D-093 | Flip spec-lint to a REQUIRED status check at the Phase-1 gate, exempting ONLY the `\| Stories \|` field from the `[filled by ...]` rule until Phase 2. Resolves a previously-unrecorded contradiction: D-029/D-032's flip was UNSATISFIABLE as written — all 25 `[filled by ...]` occurrences are in the `\| Stories \|` field and stories cannot exist until Phase-2 decomposition. **LANDED ON DEVELOP as d4e76fa (PR #9 merged).** | Operator ruling this session. D-029/D-032 recorded "flip to REQUIRED at Phase-1 gate" without acknowledging that the 25 remaining legitimate placeholders are the Stories field only. The carve-out makes the flip satisfiable without an allowlist. | phase-1d | 2026-08-07 | human/operator |
| D-094 | Session wrap — durable RESUME SNAPSHOT D-094 committed to factory-artifacts, superseding D-090 | Zero-context resume; single-commit burst TD-VSDD-053; BI-025/BI-026 CLOSED; id-resolution 10→0; em-dash 55→53; prd.md v1.12 | phase-1d | 2026-08-08 | state-manager |
| D-095 | Pass 7 DEFERRED — dispatch ONLY after (1) PR #10 merged, (2) 53 em-dash rows rewritten to `test-sufficient` sentinel (cross-checked against VP-INDEX), (3) D-077 burn-down verified clean by repaired checkers (`check-placeholders` must reach 0). Pre-dispatch operator gate still stands. Streak re-counts from ZERO against frozen HEAD. Severity reporting in RANGES, not point totals (PG-012). | Gate #30 operator ruling. Running pass 7 against a state where the 53 em-dash rows remain would re-find the same items rather than measure genuine spec convergence. | phase-1d | 2026-08-08 | human/operator |
| D-096 | PR #9 (feature/pol14-test-sufficient) B1 mandatory remediation: add selftest P14-8 (passes `""` to `is_conforming_vp_cell`, asserts `false` — kills M4), add selftest P14-9 (Shape 2 with `--allow` flag — kills M8), re-run mutations M4 and M8 and confirm BOTH DIE, apply S1 (reset `current_h2_heading` on an ATX heading of ANY level and/or require bullet to name the Stories field, so `- Architecture Module: [filled by architect]` under `## Story Anchor` is not silently exempt). Obtain FRESH review verdict at new head — explicitly NO `covered_sha` shortcut, no carry-over of the `87cefbf` verdict. Merge on APPROVE via gate-#28 mechanism. | Gate #30 operator ruling. B1 finding in PR #9 review (`code-delivery/BI-POL14-TEST-SUFFICIENT/pr-review.md`): M4 and M8 survive Shape 2 at 62/62 exit 0 with zero selftest coverage; a no-op `is_conforming_vp_cell` returning `true` passes all selftests. Fresh verdict required because the new head will carry different code. | phase-1d | 2026-08-08 | human/operator |
| D-097 | BI-046 (reviewer identity) authorized to resolve NOW via the normal review lifecycle. If its resolution requires a policy or scope ruling rather than a mechanical fix, escalate as a gate instead of deciding unilaterally. Required before Phase 3 story PRs begin (pre-Phase-3 condition, D-089). | Gate #30 operator ruling. BI-046 is demonstrably blocking — structural gap in PR approval independence that must be closed before Phase 3. Authorized now rather than deferring to avoid it remaining a dormant obligation. | phase-1d | 2026-08-08 | human/operator |
| D-098 | Burst-24 session/burst wrap — RESUME SNAPSHOT D-098 committed to factory-artifacts, superseding D-094 | PR #9 APPROVED at a42e155 (68/68; six mutants die: MS1/MX1/MX3/MB/M4/M8; 4/4 CI green) — MERGE GATED TO OPERATOR; 53 em-dash rows verified ACTIONABLE (exact bijection VP-INDEX test-sufficient set; 33 BC files); S3 must land before 53-row rewrite (D-100); BI-046 escalating as gate (operator infrastructure decision); validate-pr-review-posted hook misfire and cycle-3 reviewer boundary probe disclosed and contained | phase-1d | 2026-08-08 | state-manager |
| D-099 | `verify-sha-currency.sh` wrap step FORMALLY RETIRED for this project — orchestrator's direct git-inspection plus checker re-runs is the accepted stronger substitute. Record as factory-engine packaging gap (CI-063 class) for the plugin; stop recording it as a silent per-wrap caveat | Operator ruling. The hook is absent from this project; carrying it as a per-wrap gap produced no actionable outcome. Formal retirement stops it recurring as a per-wrap caveat. | phase-1d | 2026-08-08 | human/operator |
| D-100 | S3 (require non-empty Proof Method alongside `test-sufficient`) MUST land before the 53 em-dash rows are rewritten — S3 becomes live exactly when those rows migrate off `—` | If S3 is skipped, `test-sufficient` silently accepts empty Proof Method, allowing a row to assert "tests are sufficient" while naming no test at all (D-078 violation). Ordering: PR #9 merge → S3 → 53-row rewrite. One-line fix; no blocking technical dependency beyond PR #9 merging. | phase-1d | 2026-08-08 | human/operator |
| D-101 | Session wrap — durable RESUME SNAPSHOT D-101 committed to factory-artifacts, superseding D-098 | PR #9 MERGED (d4e76fa; D-092/D-093 on develop; check-placeholders 78→53); PR #10 OPEN+APPROVED (a14711e, S3, 69/69, 4/4 CI green) — MERGE GATED TO OPERATOR; 53-row rewrite verified fully actionable (VP-INDEX bijection + all 53 carry non-empty Proof Method); BI-046 DEFERRED pre-Phase-3 (D-104) | phase-1d | 2026-08-08 | state-manager |
| D-102 | PR #9 merge approved conditional on correcting the stale PR body first — body claimed 55 findings/62 tests and omitted the 6 mutations that mattered; corrected to 53 findings/68 tests + full mutation table before merge | Operator decision. Stale body would falsify the audit record. Executed: body corrected, then merged as d4e76fa. | phase-1d | 2026-08-08 | human/operator |
| D-103 | S3 fix landed as a follow-on PR #10 rather than added to PR #9 — adding it would have required a 4th review cycle and a cycle-limit exception | Operator decision. PR #9 was at cycle limit; S3 is a one-line fix with no blocking technical dependency beyond PR #9 merging. | phase-1d | 2026-08-08 | human/operator |
| D-104 | BI-046 (reviewer identity) DEFERRED to pre-Phase-3 — merges remain operator-gated until resolved per D-089's pre-Phase-3 condition | Operator decision. BI-046 manifested as a live enforcement control this session (auto-mode classifier blocked combined review+merge dispatch on self-approval grounds). Deferral acknowledges the gap; pre-Phase-3 is the deadline. | phase-1d | 2026-08-08 | human/operator |
| D-105 | `validate-pr-review-posted` hook has two structural defects: (1) demands `gh pr review --approve/--request-changes` which GitHub structurally CANNOT record on self-authored PRs; (2) matches literal `pr-review.md` conflicting with SHA-keyed multi-cycle immutable-audit convention. PR-lifecycle step-counter misfired by counting only current-turn STEP_COMPLETE emissions rather than full lifecycle. Classified as factory-engine packaging gaps, CI-063 class. Effective mitigation: in-prompt warning until hooks fixed. | ~12 misfirings across three agents this session; two structural defects, both well-evidenced. Agent behaviour under misfire was correct in the two later cases (refused to fabricate verdict, refused to overwrite audit records). D-082 discipline boundary: cycle-3 reviewer attempted `gh pr review --approve` to convert "impossible" into an executed predicate; permission classifier blocked it; orchestrator confirmed no approval was ever recorded. | phase-1d | 2026-08-08 | orchestrator |
| D-106 | Session wrap — durable RESUME SNAPSHOT D-106 committed to factory-artifacts, superseding D-101 | Zero-context resume; single-commit burst TD-VSDD-053; PR #10 MERGED (f8ee4eb) + 53-row sentinel rewrite COMPLETE (671109d) + D-077 burn-down CLEAR + full spec-lint GREEN (9/9) — first fully-green spec-lint in the run | phase-1d | 2026-08-08 | state-manager |
| D-107 | PR #10 (`fix/s3-test-sufficient-proof-method`) squash-merged as f8ee4eb; develop advanced d4e76fa→f8ee4eb; S3/D-100 non-empty Proof Method precondition now live on develop; remote branch deleted; exactly two worktrees remain | Operator merged after independently verifying APPROVE comment, green required checks, and scope limited to `check-placeholders.py` + `run-selftests.sh`. Closes review finding S3 per D-100. Post-merge selftests 69/69 exit 0; P14 header count 14; S3 guard text present. | phase-1d | 2026-08-08 | human/operator |
| D-108 | 53-row VP-column sentinel rewrite COMPLETE — 33 BC files modified, 53 `—` cells replaced with `test-sufficient`, 33 version fields bumped +0.1; diff purity verified (`git diff --shortstat a306ed6..671109d` = 33 files, 86ins/86del = exactly 53 sentinel cells + 33 version lines, ZERO other additions); em-dash corpus delta exactly −53 (1922→1869); anti-vacuity proven by orchestrator: revert one cell → check-placeholders reports exactly 1 finding; restore → 0. **check-placeholders=0; D-077 burn-down CLEAR; full spec-lint gate GREEN (9/9) — FIRST fully-green spec-lint in the entire run.** All 9 checkers independently re-run. **SCOPE CORRECTION (D-113):** check-placeholders=0 SCOPED to 133 of 134 spec files; prd.md excluded wholesale by unguarded EXCLUDE_PATHS skip-set (see BI-047); 1 unadjudicated `[filled by]` in prd.md; burn-down zero is scope-qualified, not absolute. | Factory-artifacts HEAD 671109d. No prose, other table columns, or Traceability rows altered. Per-row legality re-confirmed: all 33 BCs classified test-sufficient in VP-INDEX; all 53 rows carried non-empty Proof Method; S3 rejected none. | phase-1d | 2026-08-08 | orchestrator |
| D-109 | spec-lint REQUIRED flip (D-029/D-032/D-093) is now MECHANICALLY SATISFIABLE for the first time — check-placeholders=0 with D-093 Stories-field carve-out in force; all 25 `[filled by ...]` occurrences are in the Stories field which is exempt until Phase 2. Flip SCHEDULED for Phase-1 human approval gate; NOT executed this session. | Previously UNSATISFIABLE AS WRITTEN because all 25 remaining `[filled by ...]` placeholders were in the Stories field which cannot be filled before Phase 2 decomposition. D-093 resolved the structural contradiction with the carve-out; D-108 delivered the zero that makes the flip satisfiable. | phase-1d | 2026-08-08 | orchestrator |
| D-110 | Session wrap — durable RESUME SNAPSHOT D-110 committed to factory-artifacts, superseding D-106 | Zero-context resume; single-commit burst TD-VSDD-053; pass-7 authorization recorded; BI-047 OPENED (EXCLUDE_PATHS skip-set; prd.md excluded wholesale); D-108 burn-down scope CORRECTED (D-113); 0 of 3 clean passes UNCHANGED | phase-1d | 2026-08-08 | state-manager |
| D-111 | Pass 7 AUTHORIZED at gate #31; frozen perimeter = `specs/` tree hash `ace1745871122cd1fa2c46cf27c5493cc1083411`; last commit touching `specs/`: `671109d`; factory-artifacts at authorization: `cf0c5c7`; develop: `f8ee4eb`; scripts/spec-lint tree: `f2392b456c3bd669e3efbe86b3e6eae62e302ed1`; perimeter size: 134 `.md` files under `specs/`, 66 BCs + BC-INDEX.md; D-095 preconditions independently verified by operator (PR #9+#10 MERGED, zero open PRs, develop 4/4 checks green, spec-lint advisory-red accepted) | Tree hash chosen over commit SHA because state-only bookkeeping commits move the branch SHA without changing the spec perimeter. The `specs/` tree was byte-identical at `671109d` and `cf0c5c7`; this wrap commit will not change it. Any future commit whose `specs/` tree still hashes to `ace1745…` is the SAME perimeter. | phase-1d | 2026-08-08 | human/operator |
| D-112 | Pass 7 dispatch DELIBERATELY DEFERRED on context-grounds — orchestrator at ~420K of 430K wrap ceiling; D-037 requires orchestrator to hold entire findings set in context to persist the report; pass 6 produced 259 findings; dispatching into ~10K of headroom risked losing the pass findings mid-record (same failure class as pass-3: 29 of 39 findings stored as stubs per D-037); standing directive is to wrap at a clean boundary when approaching 430K; pass 7 MUST be the FIRST substantive act of the next session with a full context window | Past failure evidence: pass-3 stored 29 of 39 findings as stubs requiring orchestrator-side recovery per D-037. Context-ceiling deferral is not a block; authorized at gate #31; all pass-7 constraints carry forward verbatim (D-111). | phase-1d | 2026-08-08 | orchestrator |
| D-113 | Burn-down scope correction: D-108's check-placeholders=0 holds for 133 of 134 spec files; prd.md EXCLUDED WHOLESALE by unguarded EXCLUDE_PATHS skip-set in check-placeholders.py (D-039 class); SUPPRESSION_PATTERN guard misses EXCLUDE_PATHS by vocabulary (enforces ALLOWLIST/SKIP_LIST/etc., not the CONCEPT); prd.md contains 1 unadjudicated `[filled by]` occurrence; 133 vs 134 file-count discrepancy across sibling checkers was the visible tell; D-108 characterization SCOPE-QUALIFIED (not retracted); D-077 zero remains valid for 133 files. Standing lesson: a green oracle must be audited for its own scope before its zero is trusted. | Structural gap: a skip-set that evades the guard is more serious than a single instance. D-081 established the correct mechanism (position-based function, not named set). Correct fix: replace EXCLUDE_PATHS with a position-based scoping function per D-081; BROADEN SUPPRESSION_PATTERN to catch the concept not a fixed vocabulary. | phase-1d | 2026-08-08 | orchestrator |
| D-114 | Session wrap — durable RESUME SNAPSHOT D-114 committed to factory-artifacts, superseding D-110. | Zero-context resume; single-commit burst TD-VSDD-053; pass 7 COMPLETE (9 shards); BI-046 CLOSED (D-120); BI-048..BI-054 (exhaustive) OPENED; D-116 perimeter-closure retracted; D-117 spec-lint flip CONTRA-INDICATED. | phase-1d | 2026-08-08 | state-manager |
| D-115 | Adversary pass 7 COMPLETE — 9 shards, 273–275 findings / 45 CRITICAL / 20 process-gap tags against frozen specs/ tree ace1745…. Single severity vocabulary CRITICAL/HIGH/MEDIUM/LOW (PG-012). Verdict NOT CLEAN; clean-pass streak remains 0 of 3. | Human-ratified gate #31 (D-111); first substantive act of session per D-112; harness persisted 9 shard JSONs directly per D-118. | phase-1d | 2026-08-08 | orchestrator |
| D-116 | **PERIMETER-CLOSURE CLAIM RETRACTED.** Pass 6 synthesis asserted "all 66 of 66 BC bodies" read; predicate proved **55 of 66** BC bodies and **21 of 26** VP bodies. Never-read set: SS-05 (BC-2.05.001/002/003), SS-06 (BC-2.06.001), SS-10 (BC-2.10.002/003/004/007/008/009/010). Pass 7 closes the gap: shard 9 read the 11 never-read BC bodies; shard 7 read all 26 VP bodies. | Coverage claims must be audited against the union of shard scopes, not accepted from the synthesis. Generalises D-113. | phase-1d | 2026-08-08 | orchestrator |
| D-117 | **spec-lint REQUIRED flip is CONTRA-INDICATED and must NOT be executed at the Phase-1 gate (D-109).** Pass 7 proved three enforcement hooks structurally blind with live in-corpus breaches: `check-holdout-boundary.py` blind to prose (EC-151 leak at prd.md:616); `check-adr-consistency.py` never reads BCs/test-vectors (phantom codes malformed-fragment, E-IO-002, E-CLI-001); `check-ec-injectivity.py` cannot detect BC-to-registry scenario mismatch (systemic BI-051 class). Flip DEFERRED until BI-047/049/050/051 repaired. | D-072 error of letting a verifier with known-open bypasses be the oracle for its own fixes. | phase-1d | 2026-08-08 | orchestrator |
| D-118 | **D-037 report-persistence gap MITIGATED.** Harness persisted each adversary subagent output to a JSON file on disk; state-manager read those files DIRECTLY — 275 findings across ~660KB persisted verbatim without transiting the orchestrator context. Recommend codifying as the standard adversarial-report persistence path in the factory engine. | D-037 required orchestrator to hold all findings in context, causing pass-3 stub loss and D-112 context-ceiling deferral. | phase-1d | 2026-08-08 | orchestrator |
| D-119 | DEV-11 CONFIRMED: the Phase-3 wave-1 wave gate is Run A's target endpoint; at that gate the human will be re-asked stop-vs-continue. No work to be scheduled beyond it. | Human/operator ruling. | phase-1d | 2026-08-08 | human/operator |
| D-120 | BI-046 CLOSED: no separate reviewer identity will be provisioned. Operator-confirmed merges via gate #28/#31 mechanism are the standing two-party control for the remainder of the run. Supersedes D-104 deferral and discharges D-089's pre-Phase-3 condition. | Human/operator ruling. | phase-1d | 2026-08-08 | human/operator |
| D-121 | Session wrap; durable RESUME SNAPSHOT D-121 committed to factory-artifacts, superseding D-114. | Zero-context resume capability; single-commit burst TD-VSDD-053; session-closing state burst after gate #34 oracle repairs delivered as PR #11. | phase-1d | 2026-08-08 | state-manager |
| D-122 | Operator gate #34 rulings journaled: (1) remediation sequencing = ORACLES FIRST per D-072 — repair checkers, re-run for true mechanical baseline, then content; oracles expected to WIDEN count before it shrinks; (2) EC-151 BURN to visible test + fresh hidden replacement authored, per D-020 precedent — leaked blindness is unrecoverable; (3) spec-lint REQUIRED flip DEFERRED per D-117 until four checkers repaired — no false-green oracle becomes a merge gate; DEV-11 ruling unchanged: Phase-3 wave-1 wave gate remains Run A endpoint. | Gate #34 operator rulings delivered in prose, journaled as decisions per standing protocol. | phase-1d | 2026-08-08 | human/operator |
| D-123 | Four structurally-blind spec-lint oracles REPAIRED on `fix/oracle-repairs-gate34` (14 commits, head `b2f55ac`), delivered as PR #11. BI-047: EXCLUDE_PATHS removed, replaced by two D-081 position-based predicates (changelog-narrative H3 scoping; backtick-span detection); `check-placeholders` now 134/134 (complete), closing 133-vs-134 discrepancy. BI-049: POLICY 18 broadened to prose with suffix-bound predicate binding input/expected-output pair to specific EC ID. BI-050: POLICY 19 broadened from ADR-only to corpus-wide; closed set read from `error-taxonomy.md` at runtime. BI-051: BC-vs-registry scenario comparison added (divergent / requires-adjudication / pass). Selftests 69→91/91, all mutation-verified. `.factory/specs/` UNTOUCHED across 14 commits (tree ace1745… intact); pass-7's 273–275 findings remain valid. | Oracle repairs are prerequisites to content remediation per D-072/D-117; repaired oracle output is the true mechanical baseline (D-124). | phase-1d | 2026-08-08 | orchestrator |
| D-124 | **TRUE MECHANICAL BASELINE established** (gate #34 deliverable). Prior "9/9 green / full spec-lint GREEN" (D-108) was an artifact of blind oracles. Post-repair: 6 checkers pass, 3 fail — `check-ec-injectivity`: 17 divergent + 43 requiring adjudication (149 EC citations); `check-adr-consistency`: 26 violations (292 reason-code occurrences); `check-holdout-boundary`: 1 violation (EC-151 prose leak, `prd.md:616`). Passing: canonical-facts (31 bindings/11 facts), placeholders (0 across 134 of 134 complete), counts (37), id-resolution (134 files), index-integrity (80), title-sync (66 titles). Every checker asserts 134/134 spec files at runtime. 44-finding + 43-adjudication baseline scopes content-remediation workstream. | D-108's "GREEN" was structurally invalid once blind oracles were repaired. The TRUE baseline is the post-repair output. | phase-1d | 2026-08-08 | orchestrator |
| D-125 | Selftest suppression guard criterion CORRECTED: "no scope reduction" → "no UNPROVEN scope reduction". A checker may narrow scope ONLY if it emits a runtime assertion proving parts sum to full corpus. Detection now concept-based (not fixed vocabulary): EXCLUDE_PATHS is semantically identical to SKIP_LIST and was wholly invisible to the old guard — this is why BI-047 survived. False claim that guard "closes the class structurally" corrected in `run-selftests.sh` and `spec_lint_primitives.py`. Guard pre-flight scans all 15 checkers/generators; reports 0 unproven scope reductions. Generalises D-113. | BI-047 survived because the guard enforced a fixed vocabulary, not the concept. Concept-based detection closes the entire class. | phase-1d | 2026-08-08 | orchestrator |
| D-126 | `check-ec-injectivity` CALIBRATION LIMITATION recorded, not hidden. Jaccard-similarity buckets surface 17 divergent + 43 adjudication of 149 citations (~40%); adversary pass 7 measured mis-anchoring by semantic reading at higher rates (11/12 SS-06; 16/24 shard 4; 19/36 shard 5; ~64%). Checker is SOUND — no false-green, undecidable cases routed to explicit `requires-adjudication` bucket — but LESS SENSITIVE than semantic review. Mechanical output is a LOWER BOUND on the mis-anchoring class; must not be treated as its full extent. Threshold re-calibration is a follow-up. | Honesty about calibration limits is as important as fixing blind spots. A lower-bound oracle is better than a false-green oracle. | phase-1d | 2026-08-08 | orchestrator |
| D-127 | **INFRASTRUCTURE DEVIATION, human-ordered: repository visibility changed PRIVATE → PUBLIC.** Executed `gh repo edit BOHICA-LABS/mdlinkcheck-cloud --visibility public --accept-visibility-change-consequences`. Completion timestamp: 2026-08-08T22:55:24Z (UTC), verified by orchestrator. Rationale: GitHub Actions billing-blocked (BI-055) — every job exited in 2–4s with billing annotation, structurally preventing all four required status checks from running and therefore blocking every PR. Public repos use free GitHub-hosted runners, removing the billing gate. **Secret pre-clearance:** operator pre-scanned; orchestrator independently corroborated — `.mcp.json` is fully env-var interpolated (`${PERPLEXITY_API_KEY}`, `${TAVILY_API_KEY}`, `${CONTEXT7_API_KEY}`, no literal values); scan of both `develop` and `factory-artifacts` trees for GitHub tokens (`ghp_`/`gho_`/`github_pat_`), OpenAI-style keys, AWS access-key IDs, PEM private-key headers, and Slack tokens returned ZERO matches. **WALL-TIME BOUNDARY for CI comparisons:** timings before 2026-08-08T22:55:24Z are private-runner; at/after are free public runners. NFR-001/NFR-002/NFR-008 baselines MUST NOT be compared across this boundary without re-baselining. Note: agent died on stream-idle API error after the flip succeeded; orchestrator completed verification directly. | BI-055 resolution; CI billing gate removed by visibility change. | phase-1d | 2026-08-08 | human/operator |
| D-128 | **Post-visibility-change verification PASSED — branch protection survived intact on both branches.** Verified by orchestrator immediately after the flip. `develop`: required status check contexts exactly `["Format check","Clippy (deny warnings)","Test (macos-latest)","Build release (macos-latest)"]`, `strict: true`, `required_linear_history: true`, `allow_force_pushes: false`, `allow_deletions: false`, `required_approving_review_count: 0`, `enforce_admins: false`. `main`: identical plus `enforce_admins: true`. The `required_approving_review_count: 0` value is LOAD-BEARING per D-021/D-089/BI-039 — every PR authored by `drbothen`; GitHub structurally forbids self-approval; any nonzero value would deadlock every PR; it survived the visibility change unchanged. CI liveness confirmed on PR #11 (head `b2f55ac`): all four required checks now PASS; advisory `Spec lint` executes for 9–11s and reports a real failure (per D-124) rather than dying in 2–4s with a billing annotation. The `Spec lint` failure is CORRECT AND INTENDED per D-124. PR #11 is now merge-eligible on CI grounds — 4/4 required checks PASS — AWAITING OPERATOR-CONFIRMED MERGE per D-120. | Branch-protection integrity confirmed post-visibility-change; CI liveness proven on PR #11. | phase-1d | 2026-08-08 | orchestrator |
| D-129 | PR #11 MERGED as squash commit `da86271` at 2026-08-08T23:49:52Z after operator-confirmed merge (gate-#28 mechanism; operator independently verified cycle-5 APPROVE freshness at head `b2f55ac`, 4/4 required checks green, and corpus non-modification). `develop` advanced `f8ee4eb`→`da86271`. All four oracle repairs (BI-047/049/050/051) are now LIVE on develop. Post-merge verification: selftests **91/91**; frozen perimeter tree on factory-artifacts still **`ace1745871122cd1fa2c46cf27c5493cc1083411`** — guaranteed STRUCTURALLY, not merely by diff, because `.factory/` is a separate orphan-branch worktree not part of develop's tree at all. Remote and local branch `fix/oracle-repairs-gate34` deleted; worktree removed; exactly two worktrees remain. Process notes: (i) automated security warning asserted the merging subagent had merged PR #11 without human approval — orchestrator empirically DISPROVED the claim (develop was still at `f8ee4eb` when checked); merge was subsequently executed under the operator's genuine in-conversation confirmation; (ii) the valid observation retained: the pr-manager subagent edited `pr-description.md` to tick its own "operator merge authorization" checkbox immediately before attempting the merge — a D-105-class discipline boundary (an agent converting an external authorization into self-asserted evidence). Recorded as a recurring agent-behaviour pattern to watch, not a content defect. | Gate-#28 mechanism; operator-confirmed merge with empirical disproof of false-positive security warning; D-105-class pattern noted. | phase-1d | 2026-08-08 | human/operator |
| D-130 | **D-124's TRUE MECHANICAL BASELINE IS SUPERSEDED.** D-124 recorded the baseline as measured in the feature worktree BEFORE the 5 PR review cycles resolved 6 blocking findings; that tightening removed substantial FALSE POSITIVES, so the post-merge actuals on `develop` @ `da86271` are materially smaller. Corrected baseline, re-derived by orchestrator on develop post-merge: `check-adr-consistency`: **4 violations / 79 reason-code occurrences** (was 26/292); `check-ec-injectivity`: **110 EC citations compared, 9 divergent, 5 require adjudication** (was 149/17/43); `check-holdout-boundary`: **1 violation** — EC-151 prose leak at `prd.md:616` (unchanged); Passing (6): canonical-facts, placeholders (0 across 134 of 134 complete), counts (37), id-resolution (134 files), index-integrity (80 checks), title-sync (66 titles). Corrected post-merge posture: **6 pass / 3 fail, 14 mechanical findings + 5 adjudication items** — versus D-124's recorded 44+43. D-124 is SUPERSEDED, not retracted (it accurately recorded the pre-review-tightening state). **Consequence: content-remediation workstream MUST be scoped from THIS baseline, not D-124. SUPERSEDED by D-174 (first authoritative baseline on develop post-PR-#12-merge).** | D-124 was pre-review-tightening; post-merge actuals on integration branch are the authoritative baseline. | phase-1d | 2026-08-08 | orchestrator |
| D-131 | Confirmation that detection of the pass-7 calibration cases SURVIVED the merge, with one exception: `malformed-fragment` (`test-vectors.md:432`) still detected; `E-CLI-001` (`BC-2.11.004.md:61`) still detected; `EC-142` still surfaced by `check-ec-injectivity`; and a NEW true-positive surfaced — `syntax-valid` at `test-vectors.md:221` (TV-116), a reason code absent from the closed taxonomy. The exception: **E-IO-002 is NO LONGER DETECTED** — see BI-056. | Post-merge verification; E-IO-002 regression introduced by false-positive tightening during review cycles. | phase-1d | 2026-08-08 | orchestrator |
| D-132 | Standing lesson generalising D-113 and D-125: **a completeness assertion must be proven at EVERY granularity at which the checker can skip, not merely at file granularity.** `check-ec-injectivity` asserts "134 of 134 spec files (complete)" — true — while silently skipping 80 of 190 comparison rows. The D-125 guard criterion ("no UNPROVEN scope reduction") is necessary but NOT sufficient as implemented, because it validates only the file-level assertion. Extend the guard to require a completeness assertion for each skip granularity a checker employs. | Generalises D-113 and D-125; proves checker-scope-completeness claims at every skip granularity, not just file level. | phase-1d | 2026-08-08 | orchestrator |
| D-133 | **Operator gate #35 rulings.** (1) Resequencing APPROVED: close BI-056 + BI-057 BEFORE EC-151 burn — D-072 principle (known false negative + 42% unproven row exclusion disqualifies oracle from scoping content work). Authoritative queue: **(0) BI-056 + BI-057 → (1) EC-151 burn + fresh hidden replacement → (2) BI-052 → (3) BI-053 → (4) BI-054 → (5) adversary pass 8** (streak counts from ZERO). (2) D-130 SUPERSEDES D-124 as authoritative mechanical baseline; content workstream scoped from D-130, NOT D-124. (3) D-132 ENDORSED as standing requirement and EXTENDED to ALL NINE CHECKERS before pass 8 may rely on them. (4) Delivery split confirmed: checker code changes through full PR lifecycle; artifact/spec repairs commit directly to factory-artifacts. | D-072 principle: false negative + 42% unproven row exclusion disqualifies oracle from scoping content work. | phase-1d | 2026-08-09 | human/operator |
| D-134 | Session wrap; durable RESUME SNAPSHOT D-134 committed to factory-artifacts, superseding D-121. Wrap taken at a clean boundary on context grounds (D-112 discipline): PR #11 merged and cleaned, zero open PRs, exactly two worktrees, both branches synced with origin, all findings and rulings durably recorded. No work started that could not be completed. | Clean-boundary wrap prevents partial-work loss class (D-037). Same discipline as D-112 and D-030. | phase-1d | 2026-08-09 | orchestrator/state-manager |
| D-135 | **Operator gate #36 ruling 1 — PR #12 scope.** Fold the E-code gaps (AC-1/AC-2/AC-3/AC-7) into PR #12; route ADRs through the broad-corpus path so Pattern 4 + occurrence counting apply uniformly. Count and disclose the frontmatter skip and the dedup skip so the total reconciles to 8. "BI-056 closes honestly or not at all." | D-072 oracle-integrity principle: a checker that silently routes some corpus members through a narrower path cannot be the oracle for its own completeness. | phase-1d | 2026-08-09 | human/operator |
| D-136 | **Operator gate #36 ruling 2 — Guard rebuild depth.** Coverage ledger + anti-tautology check across all nine checkers, PLUS the HS-INDEX `required_files` fix and the `run_suppression_guard` Pass-1 fail-open fix are REQUIRED. AST walk and narrow-literal detector EXPLICITLY SKIPPED — the narrow-literal class (BI-056) stays on reviewer vigilance, accepted because the checker layer is a candidate for structural replacement post-run and pass 8 is the schedule priority. Residual risk to be documented explicitly. D-132's proof obligation satisfied by the ledger (per D-132 + D-133), not the AST layer. | Operator decision: checker layer is a replacement candidate; AST layer is disproportionate investment at this stage. Accepted residual documented in d132-checker-audit.md. | phase-1d | 2026-08-09 | human/operator |
| D-137 | **BI-056 fold DELIVERED at `fd74bd7`**, closing four audit-identified defects in `check-adr-consistency.py`: AC-3 (ADR path never ran Pattern 4 — the BI-056 fix had never reached that code path); AC-2 (ADRs contributed 0 occurrences while counting toward "134 of 134 complete" — the BI-057 signature in a sibling checker); AC-1 (YAML-frontmatter exclusion silent, hiding 2 of 8 corpus E-class occurrences; exclusion itself legitimate, invisibility was the defect); AC-7 (Pattern-4 dedup let Pattern 3 claim `E-CLI-001` first and validate it against the wrong registry). E-class detections 5→6. POLICY 12 coverage preserved — `check_adr()` textually unchanged. | AC-3 was the critical miss: the original BI-056 fix never reached the ADR code path, so POLICY 12 was still checked but E-class detection was absent from ADR files. | phase-1d | 2026-08-09 | orchestrator |
| D-138 | **BLOCKING-1 in PR #12 cycle-2 review: E-class reconciliation was a TAUTOLOGY.** `e_population` was DEFINED as `total_e_occurrences + total_frontmatter_e_skipped` then asserted equal to that same expression — literally `x == x`, unfalsifiable for all inputs. Proven empirically by the reviewer and INDEPENDENTLY RE-VERIFIED by the orchestrator: injecting an undeclared table-line skip before the Pattern 4 loop dropped E-code detections 6→3 and violations 9→6 on the live corpus while the reconciliation still closed (`5 = 3 + 2`), the summary still printed `134 of 134 spec files (complete)`, and ALL FOUR new selftests still passed. Secondary defect: `assert` is stripped under `python -O`, while the three sibling completeness gates in the same function correctly use `print(...) + return 2`. **Standing lesson: a population derived from the same matching machinery and routing path it is meant to audit proves nothing; closure of a ledger is NOT evidence of coverage.** | The acceptance criterion required "a hard runtime assertion that 8 == E + S" and "population derived, not hardcoded" — BOTH were satisfied by the tautology, proving acceptance criteria for completeness gates must require a demonstrated FAILING case. | phase-1d | 2026-08-09 | orchestrator |
| D-139 | **BLOCKING-2 in PR #12 cycle-2 review: ADR reason-code violations double-reported.** Calling `check_broad_corpus()` on ADRs in addition to `check_adr()` ran two independent POLICY 19 reason-code detectors over the same tokens, so one ADR defect produced two violation entries. Zero live impact (0 ADR reason-code violations today, published `9` correct) but `len(violations)` — the baseline metric this PR ships downstream — was no longer injective. Fixed by restricting the ADR path to E-class detection only (`e_class_only` mode); measured-zero loss. Deliberate disclosed residual: ADRs do not receive Pattern 2/verdict-parenthetical or Pattern 3/taxonomy-reference detection (they never did pre-fold either). | "Do not lose POLICY 12" instruction produced additive routing and a non-injective violation count — the inverse defect class: guarding against losing coverage created double-counting. | phase-1d | 2026-08-09 | orchestrator |
| D-140 | **BLOCKING-3 in PR #12 cycle-2 review: evidence bundle stale by two commits.** The headline deliverable (E-class population reconciliation) appeared in NO committed evidence artifact at the time of cycle-2 review. Evidence refreshed at new head `ca8c1c0` with an AC exercising the reconciliation and its fail case. | Evidence must reflect the actual head being reviewed, not a prior state. D-130's standing lesson applies: provisional baselines stale faster than the review cycle. | phase-1d | 2026-08-09 | orchestrator |
| D-141 | **Gate #39 operator ruling + AMENDMENT to gate #36 ruling 2 (D-136 AMENDED — operator's own framing).** All three cycle-2 BLOCKING fixes APPROVED, including print+return-2 over assert, the mutation-verified selftest injecting an undeclared skip, and the NIT-1 table-cell fixture (a nit that causally explains a surviving mutant is not a nit). **Ruling 2 AS AMENDED now reads: coverage ledgers with INDEPENDENT-PROBE (canary) populations, deliberately WIDER than the detector, are MANDATORY for all nine checkers; with that constraint the ledger-over-AST decision stands.** Rationale: without independent-probe canaries the nine-checker sweep would manufacture nine tautologies — the exact class the sweep exists to eliminate. | Without the independent-probe constraint, D-136's "coverage ledger" requirement could be satisfied by a ledger whose population is derived from the same matching machinery it audits (D-138 tautology class). The amendment closes this loophole for all nine checkers. | phase-1d | 2026-08-09 | human/operator |
| D-142 | **BLOCKING-1 fix VERIFIED by orchestrator directly (not agent self-report).** With the independent canary in place, the same table-line mutant now yields `ERROR: E-class accounting gap — population=8 != examined=3 + skipped=2; an E-class occurrence is being dropped by an undeclared routing path`, **exit 2**. Unmutated: `population=8, examined=6, skipped=2 (frontmatter, D-081)`, gate passes. | Direct orchestrator verification, not agent self-report, closes the D-138 class for this checker. Pattern for the nine-checker sweep: each checker's canary must be verified by an independent probe that FAILS on the target defect class. | phase-1d | 2026-08-09 | orchestrator |
| D-143 | Session wrap; durable RESUME SNAPSHOT D-143 committed to factory-artifacts, superseding D-134. Burst covers gates #36-#39, PR #12 lifecycle (ca8c1c0; cycle-3: 0 BLOCKING, 2 NITs; security APPROVE), BI-056/BI-057 repairs, D-132 audit record, BI-058 opened, CI-063 recorded. | Clean-boundary wrap. PR #12 awaiting operator-confirmed merge (D-120). Nine-checker ledger sweep is the next burst post-merge. Same discipline as D-112, D-030. | phase-1d | 2026-08-09 | state-manager |
| D-144 | Gate #40 ruling: PR #12 cycle-4 LGTM WITHOUT BLOCKING — result confirmed by orchestrator direct verification of the review file, NOT agent self-report. Review delivered 1 BLOCKING (BLOCKING-C: verify-evidence-figures.py was absent from audit files) and 4 NITs. Gate #40 established: result of a review pass MUST be recorded in the factory-artifacts review file before it exists; an agent's summary of what it did is not the finding. | D-129 class (relaying agent self-report as fact): BLOCKING-C was unrecorded in any audit file when first claimed resolved; orchestrator went to the file and confirmed the recording gap. Lesson 53: an unrecorded result does not exist. | phase-1d | 2026-08-09 | orchestrator/operator |
| D-145 | Gate #41 ruling: three consecutive fix passes on the same `verify-evidence-figures.py` file (cycles 4/5/6) constitute an APPROACH DEFECT, not a quality defect. Root cause: fixes were addressing symptoms (specific fail-open paths) rather than the structural property (script fails open on ANY unexpected code path). Gate #41 required a step back to verify the structural property, not patch the known shapes. | D-066 precedent (four consecutive failures = design signal). Three consecutive = approach defect; further patching is strictly worse than a structural fix. | phase-1d | 2026-08-09 | human/operator |
| D-146 | Gate #42 ruling: nine-checker ledger sweep AUTHORIZED. Scope per D-141 amended ruling 2: coverage ledger with INDEPENDENT-PROBE canary populations + anti-tautology for all nine checkers + HS-INDEX `required_files` fix + `run_suppression_guard` Pass-1 fail-open fix. Authorization embedded in each implementer's INITIAL SPAWN PROMPT per CI-063 mitigation. Pre-condition: fix BLOCKING-D/E in `scripts/verify-evidence-figures.py` first (BI-059). | The nine-checker sweep is the next burst after BLOCKING-D/E are resolved. CI-063 mitigation: embed operator authorization verbatim in each implementer's spawn prompt; never rely on mid-session relay. | phase-1d | 2026-08-09 | human/operator |
| D-147 | PR #12 cycles 1-5 ALL RESOLVED as of head `f6dfa58`. Final tally: 10 commits on `fix/checker-completeness-gate35`; 99/99 selftests all mutation-verified; 4/4 CI green. All prior BLOCKING findings (cycle 1: B-1; cycle 2: BLOCKING-1/2/3; cycle 3: 0; cycle 4: BLOCKING-C) resolved. Cycle-5 review: verdict LGTM-no-blocking, confirmed by orchestrator direct-verification. Cycle-6 review opened: BLOCKING-D/E in `verify-evidence-figures.py`. | Cycles 1-5 resolution is a prerequisite milestone for gate #42. Cycle-6 opened two new structural blockers (BLOCKING-D/E). | phase-1d | 2026-08-09 | orchestrator |
| D-148 | BLOCKING-D: `scripts/verify-evidence-figures.py` FAILS OPEN. Three structural defects: (1) Checks 2, 3, 4 use bare `if match:` with NO `else: fail(...)` — any unexpected code path silently passes; (2) `sh()` helper lacks `check=True` — subprocess failure returns silently; (3) `sh()` merges stdout+stderr — error messages invisible in output parsing. This is a STRUCTURAL PROPERTY violation (the script must fail closed on ANY unexpected path), not a specific bypass to patch. Fix must prove the property, not enumerate shapes. Must include demonstrated failing case (D-138 discipline). | D-066/D-145 precedent: patching symptom instead of property. Fix must include a demonstrated failing case (same D-138 acceptance-criterion discipline). | phase-1d | 2026-08-09 | orchestrator |
| D-149 | BLOCKING-E: `scripts/verify-evidence-figures.py` Check 7 tests the WRONG PROPERTY. Check 7 verifies "PR head SHA appears in git log" (presence-in-history) rather than "content is obtainable at that SHA" (content-obtainable). A SHA can be in git log without the file being accessible at that commit. These are distinct properties; the required property is content-obtainable-at-SHA (evidence durability). | D-057 (D-050 CORR): verifying a proxy property instead of the target property is the same error class. Lesson 57: verify the PROPERTY, not a proxy. | phase-1d | 2026-08-09 | orchestrator |
| D-150 | Audit durability gap CLOSED: all 10 `code-delivery/CHECKER-COMPLETENESS-GATE35/` review files and `sidecar-learning.md` committed to factory-artifacts in this burst. Gap identified at gate #40: cycle-4 review claimed LGTM but the review file was absent from factory-artifacts — an unrecorded result does not exist (D-144/Lesson 53). | Lesson 56: a tool built to prevent a defect class can contain that class — verify-evidence-figures.py was the audit-durability verifier and itself had a durability gap (BLOCKING-D/E). | phase-1d | 2026-08-09 | state-manager |
| D-151 | Lesson 58 (validate every restatement, S-5) recorded — the orchestrator restated a claim from a prior session; the restatement introduced a factual error about the cycle count. Independent verification of session-checkpoints.md corrected the error before it propagated to the session-closing snapshot. | Same S-5 class: restating something is not re-verifying it. Applies to any handoff claim, any count, any SHA. | phase-1d | 2026-08-09 | orchestrator |
| D-152 | Lesson 59 (an over-correction is also an unverified claim) recorded — applying a fix that overshoots the defect boundary without verifying the corrected range is a different form of the D-129 error. Fix must verify BOTH boundaries: fails closed on bad input, passes on good input. Example: if patching BLOCKING-D makes the script fail closed on valid evidence, that over-correction is equally wrong as failing open. | Standing discipline for any structural fix: verify the full boundary, not just the defect side. | phase-1d | 2026-08-09 | orchestrator |
| D-153 | Session wrap; durable RESUME SNAPSHOT D-153 committed to factory-artifacts, superseding D-143. Burst covers gates #40-#42, PR #12 cycle-6 (f6dfa58; 99/99 selftests; cycles 1-5 ALL RESOLVED), BLOCKING-D/E OPENED (BI-059), audit durability gap CLOSED (10 files committed), D-144..D-153 (exhaustive). | Clean-boundary wrap. BLOCKING-D/E must be fixed in verify-evidence-figures.py; then re-review; then operator-confirmed merge (D-120/gate-#28); then gate-#42 nine-checker sweep. Same discipline as D-112, D-030. | phase-1d | 2026-08-09 | state-manager |
| D-154 | Gate #43: cycle-7 review APPROVE at `6a5eb9f` — 0 blocking, 4 suggestions (2 upper/2 lower: SUGGESTION-8/9/10/11), 5 nits (PG-012 ranges). `scripts/spec-lint/` subtree hash `25077be8211590e649bb37752aacaceaf88d3984` IDENTICAL at `f6dfa58` and `6a5eb9f`; cycles 1-5 adjudications mechanically preserved. Sole tracked diff `f6dfa58`→`6a5eb9f`: `scripts/verify-evidence-figures.py` (328 ins/54 del). 4/4 required CI green. Selftests 99/99. Frozen perimeter `specs/` tree `ace1745…` UNCHANGED. | Gate #43 closing review; all BLOCKING findings from cycles 1-6 now resolved; verifier hardening suggestions carried forward to sweep step 0 per D-159. | phase-1d | 2026-08-09 | pr-reviewer/orchestrator |
| D-155 | BI-059 CLOSED: BLOCKING-D closed STRUCTURALLY via `REQUIRED_CHECKS`/`checks_ran` registry — PASS gate asserts every required check registered a live-vs-document comparison; `sh()` separates stderr and gates return codes via `ALLOWED_RC`; reviewer reproduced cycle-6 stub: PASS/EXIT=0 → 8 failures/EXIT=1. BLOCKING-E closed STRONGER than prescribed patch: `git show <stamp>:<path>` with stamp line normalised away; also detects tampering with evidence artifacts (the `879efff` restamp falsification now FAILS). S-7 closed: rollback lists 12 SHAs, claims 12, checked against live count — correct for the first time in four cycles. S-5 closed for shrink class: per-figure MINIMUM restatement counts fail if coverage shrinks; live counts verified (adr-consistency 5; ec-injectivity citations 9/divergent 9/adjudication 8). | Both BLOCKING fixes proven by execution (not assertion); S-5 closed for the class that mattered; S-7 closed by performing the revert and matching trees. | phase-1d | 2026-08-09 | orchestrator/operator |
| D-156 | Two S-5 residuals adjudicated NON-BLOCKING: (1) Novel-spelling non-coverage of ADDED text — a restatement added in an unmatched spelling clears the minimums and is never compared; silent-SHRINK closed but silent non-coverage of new text is not; must NOT be fixed by pattern-chasing (unbounded, raises false-FAIL surface); correct fix is mechanism change (SUGGESTION-10: require any occurrence of live figure's digits in figure-adjacent context to be either pattern-matched or explicitly allow-listed, inverting the default from "unmatched text is invisible" to "unmatched text must be declared"). (2) Double-count in the minimums is REAL (`ev:39` matched by both `STANDARD_PAT` and per-figure patterns; reported 9/9/8 vs 8/8/7 distinct sites) but ERRS SAFE — only failure mode is false-FAIL, never false-PASS; deleting any single site still trips the minimum. | Both residuals adjudicated with evidence, not assertion; non-coverage residual requires mechanism change not pattern-chasing; double-count is safe because it only enables false-FAIL, never false-PASS. | phase-1d | 2026-08-09 | pr-reviewer (ruling) |
| D-157 | Merge AUTHORIZED-then-WITHHELD. Operator AUTHORIZED merge (option 1: merge to develop now; sweep step 0 before verifier is load-bearing). Operator then RETRACTED fallback clause: a recorded, POSTED verdict is a hard precondition (gate-#28/D-144); if automation cannot post, STOP and report — do NOT fall back to .factory-only record and do NOT merge. Three independent self-approval denials recorded (all correctly blocked): (1) reviewer spawn denied [Self Approval]; (2) operator's own harness blocked ×2 [Self Approval]; (3) pr-manager spawn denied [Self-Approval] — classifier explicitly noted operator's authorization "confirms rather than negates the self-approval pattern." Resolution path: human posts verdict directly via `gh pr comment 12 --body-file .factory/code-delivery/CHECKER-COMPLETENESS-GATE35/pr-review-cycle7.md`, then merge proceeds. NO control was weakened and NO hook or classifier was modified. | Root cause: implementer, reviewer, and pr-manager are all sub-agents of ONE pipeline — any agent-posted APPROVE on a self-authored PR is one party wearing two hats. Correct control functioning; STOP-and-report is the required response when automation cannot satisfy the precondition. | phase-1d | 2026-08-09 | human/operator |
| D-158 | BI-060 OPENED: `validate-pr-review-posted` hook (a) UNSATISFIABLE in this repo — demands `gh pr review`, structurally impossible on self-authored PRs (BI-039/D-021/D-105); (b) ACTIVELY INSTRUCTS AGENTS TOWARD SELF-APPROVAL — during cycle 7 it demanded reviewer spawn `github-ops` to run `gh pr review --approve`; reviewer REFUSED and recorded it (correct behaviour). Severity HIGH. Owner devops-engineer. Blocking-phase phase-2. Operator ruling: do not modify the hook mid-run, record and route around it by documented exception only. Cross-reference CI-063. | Hook that demands impossible action AND instructs agents toward a disallowed pattern is a higher-severity defect than a hook that merely misfires. Correct behaviour confirmed: reviewer refused and recorded. | phase-1d | 2026-08-09 | human/operator |
| D-159 | Sweep step 0 APPROVED: gate-#42 sweep begins with verifier hardening — SUGGESTION-8/9/10/11 + SUGGESTION-6 (CI wiring, operator-gated) — BEFORE `scripts/verify-evidence-figures.py` is load-bearing in the check loop. SUGGESTION-6 gated to operator. Same D-072 oracle-integrity principle: harden the verifier before it verifies anything load-bearing. | Running a verifier with known coverage gaps before hardening it would manufactures an appearance of assurance that does not exist. | phase-1d | 2026-08-09 | human/operator |
| D-160 | CI-063 EXTENDED: three additional classifier self-approval denials: (1) reviewer spawn [Self Approval]; (2) operator's own harness blocked ×2 [Self Approval]; (3) pr-manager spawn [Self-Approval] with classifier explicitly noting operator authorization "confirms rather than negates" the pattern. All three correctly blocked; no control weakened. Existing mitigation (embed operator authorization verbatim in INITIAL SPAWN PROMPT) WORKED for implementer and reviewer spawns — zero friction on `scripts/**` operations — but does NOT clear the self-approval class, which is a different, correctly-functioning control. Reviewer also recorded two of its own blocked commands (neither routed around): `cp` cycle-7 over `pr-review.md` denied [Irreversible Local Destruction] — classifier was RIGHT; `pr-review.md` verified unchanged and byte-identical to `pr-review-cycle6.md`. | Extending CI-063 with factual denials, not complaints. Self-approval control is correctly functioning; agent behaviour correct in all cases. | phase-1d | 2026-08-09 | orchestrator |
| D-161 | Burst-35 session wrap; durable RESUME SNAPSHOT D-161 committed to factory-artifacts, superseding D-153. Burst covers gate #43 cycle-7 APPROVE, BI-059 CLOSED (BLOCKING-D/E proven by execution at `6a5eb9f`), merge WITHHELD on posted-verdict precondition, BI-060 OPENED, sweep step 0 APPROVED, CI-063 extended, D-154..D-161 (exhaustive). | Clean-boundary wrap. Pickup: human posts verdict comment on PR #12, then merge, then sweep step 0 (verifier hardening). Same discipline as D-153, D-143, D-030. | phase-1d | 2026-08-09 | state-manager |
| D-162 | BI-053 CLOSED — five-source unanimity: DI-003 (`invariants.md:109`), CAP-008 (`capabilities.md:142`), DEC-005 (`edge-cases.md:132`), `events.md:92`, `prd.md:380` all state fragment MUST be percent-decoded after split. BC-2.07.004 (Description s3 + PC4) and BC-2.08.001 (PC1 + Inv3) had both FORBIDDEN decode; now corrected. The five sources DO NOT disagree — BI-053 was a contained two-BC inversion, not a wider spec defect. Versions: BC-2.07.004 1.3→1.5, BC-2.08.001 1.3→1.5. | Five independent authoritative sources unanimous; inversion was in the BCs, not in the underlying invariants. | phase-1d | 2026-08-09 | orchestrator/product-owner |
| D-163 | Boundary gap from BI-053 correction: once PC4 made fragment decoding mandatory, behaviour of UNDECODABLE percent-sequences (e.g., `file.md#caf%GG`) became unspecified — previously no rule was needed because fragments were never decoded. All five authoritative sources SILENT on undecodable fragments. Symmetry-with-path-component rule adopted: invalid sequences pass through raw, used as-is for anchor lookup, yielding `anchor-not-found`. BC-2.07.004 Inv3 extended to three surfaces (external URL → `malformed-url`; path component → `file-not-found`; fragment component → `anchor-not-found`). BC-2.08.001 gained Inv4 with same rule plus explicit exclusion of `malformed-url` (taxonomy scopes it to WHATWG URL parse failure, not fragment decode failure). Test vectors added for `%GG` case. | Symmetry with the already-specified path-component behavior; no new concepts introduced. | phase-1d | 2026-08-09 | orchestrator/product-owner |
| D-164 | Refusal to invent a reason code is a POSITIVE PRECEDENT. `anchor-not-found` (code 4 of 13-code closed set) is the correct code for undecodable-fragment outcomes. `malformed-url` would be semantically wrong (taxonomy scopes it to WHATWG URL parse failure). `malformed-fragment` would be a phantom-code regression, tripping the live BI-056 gate — adversary finding P7-S4-006 flagged exactly that pattern as CRITICAL. Correct response to a missing code: report the gap and use the closest existing code; do NOT create a new one without explicit product-owner adjudication. | 13-code closed set is authoritative; any addition is a spec change, not a fix. | phase-1d | 2026-08-09 | orchestrator |
| D-165 | Empty story-propagation target set — `.factory/stories/` contains only `.gitkeep`; zero story files exist; Phase 2 decomposition has produced no artifacts; `bc_array_changes_propagate_to_body_and_acs` has nothing to propagate to. No story-writer dispatch was required. Propagation debt RECORDED, NOT DROPPED: VP-004 (no VP asserts "fragment decoded before anchor lookup"), VP-025 (anchor-resolver harness receives anchor table externally, cannot prove decode-before-lookup), VP-INDEX consistency check for both BC changes, TV-157/EC-157 must be verified against corrected BCs. This overlaps BI-052 (false-green VP attribution class) — same defect class; handle together rather than twice. | Empty stories/ is a structural fact, not a gap to be filled here. VP propagation debt queued with BI-052. | phase-1d | 2026-08-09 | orchestrator |
| D-166 | BI-054 CLOSED — directive was the ENTIRE defect. Verified: 53 VP rows across 33 BC files already held the correct `test-sufficient` value; `check-placeholders.py` passes 0 live placeholders across 134/134 spec files both before and after; zero residual `\| none \|` in `bc-module-map.md`. Checker acceptance condition (oracle): `test-sufficient` accepted ONLY when VP-INDEX classifies the BC ID as `test-sufficient` (runtime JOIN against VP-INDEX, NOT an allowlist per D-039) AND Proof Method cell non-empty; `none` explicitly REJECTED per R2-RULE (D-069). Document's own "55 BC files" figure was wrong (actual: 53 rows / 33 files) — surfaced because D-082 forced re-derivation rather than restatement. | Directive was harmful downstream guidance; checker's oracle is the source of truth. | phase-1d | 2026-08-09 | orchestrator/architect |
| D-167 | BI-054 root cause: PROPAGATION GAP — commit `d4e76fa` added `test-sufficient` as a legal sentinel value and `bc-module-map.md` was never updated in that propagation step. Tagged `[process-gap]` per S-7.02 codification. Per S-7.02, this requires either a follow-up story targeting the self-improvement epic or a justified deferral entry with an explicit target release before the cycle closes. Do NOT close this silently. | Propagation gaps at decision time are the recurring mechanism for harmful stale directives (D-082 class). | phase-1d | 2026-08-09 | orchestrator |
| D-168 | P7-S6-009 CLOSED — `bc-module-map.md` Primary ownership table declared `Total: 66` while 16 data rows summed to 70. Four cells corrected by `awk` over BC mapping tables (D-082 executed-predicate requirement, not hand-counted): `link_extractor` 9→8, `anchor_table` 4→3, `http_client` 8→7, `cli` 4→3. Table now sums to exactly 66. CRITICAL NUANCE: Module Ownership Summary (20 lines above Primary table) counts ALL BCs touched (primary + secondary); Primary ownership table counts primary only — DIFFERENT quantities, cannot agree cell-for-cell in general. Only `http_client` is directly comparable (all 7 BCs are primary with no secondary involvement); its mismatch was a genuine regression from v1.2. Higher Summary values for `link_extractor`, `anchor_table`, `cli` are EXPECTED and correct. The original instruction to reconcile cell-for-cell was WRONG and the architect correctly refused it with reasoning. | Two tables measure different quantities; the Summary's higher numbers are structurally correct, not errors. | phase-1d | 2026-08-09 | orchestrator/architect |
| D-169 | `check-counts.py` ZERO coverage of `bc-module-map.md` — this is why P7-S6-009 drifted without detection. Correcting the arithmetic without adding coverage guarantees redrift. Coverage gap ROUTED INTO gate-#42 nine-checker sweep scope, where `check-counts` is already in scope for its two documented no-op checks and three dead functions (BI-058). Do not add `bc-module-map.md` coverage outside the sweep context — the sweep coordinates all coverage additions atomically. | Adding coverage outside the sweep would create a partial fix that the sweep's canary might miss; route together. | phase-1d | 2026-08-09 | orchestrator |
| D-170 | Pre-existing input-hash drift in `bc-module-map.md` — `validate-input-hash` PostToolUse hook fired with stored hash `c1efbb5` ≠ computed hash `0cfd557`; stale BEFORE any edit this session. Resolved via `compute-input-hash <file> --update`. Hook NOT disabled, bypassed, or weakened. Implication: input-hash drift predates this session. Full `/vsdd-factory:check-input-drift` sweep is QUEUED for after BI-052 and EC-151 land (once, not repeatedly) and is MANDATORY before the phase-1 gate per standing constraint. | Pre-existing drift must be explicitly recorded so a future burst does not misattribute it to this session's edits. | phase-1d | 2026-08-09 | orchestrator |
| D-171 | Frozen perimeter `specs/` tree ADVANCES from `ace1745871122cd1fa2c46cf27c5493cc1083411` to `a79de7e841c705a499f7aec634c4894b3097764e` with Burst-36 commit. Four spec files changed: `specs/behavioral-contracts/ss-07/BC-2.07.004.md`, `specs/behavioral-contracts/ss-08/BC-2.08.001.md`, `specs/prd.md`, `specs/architecture/bc-module-map.md`. Adversary pass 8 MUST run against the NEW perimeter hash, NOT `ace1745`. The standing "frozen perimeter `ace1745`" descriptor is SUPERSEDED as of this burst. This is authorized and expected — spec fixes are the work of phase-1d — but it must not later read as unexplained drift. | Perimeter advances are normal in phase-1d; the requirement is to document them explicitly so pass 8 knows which hash to use. | phase-1d | 2026-08-09 | orchestrator |
| D-172 | BI-060 AGENCY-vs-IDENTITY analysis: gate-#28 requirement was satisfied on the AGENCY axis (a human composed and posted verdict comment `5232703815` on PR #12 — a human typed the command) but NOT on the IDENTITY axis (PR author `drbothen` == verdict-comment author `drbothen`; machine-unverifiable distinction). The auto-mode classifier correctly notes that a human having typed the command is not machine-verifiable. This agency-vs-identity gap is the root cause of BI-039 as well and recurs on every PR in this run. Three self-approval denials + one transient stage-2 denial are recorded. No control was weakened. Burst-36 wrap; RESUME SNAPSHOT D-172. | The agency-vs-identity distinction is the mechanistic root of the recurring self-approval friction; recording it makes it diagnosable rather than mysterious. | phase-1d | 2026-08-09 | orchestrator/state-manager |
| D-173 | **PR #12 MERGED by human at 2026-08-09T18:42:19Z** — squash commit `2ac2c3e` ("[CHECKER-COMPLETENESS-GATE35] Spec-lint oracle repairs — BI-056 E-code detector + BI-057 multi-column TV extraction (#12)"). `develop` advances `da86271`→`2ac2c3e`. Branch `fix/checker-completeness-gate35` DELETED. Zero open PRs at merge. `scripts/spec-lint/` subtree preserved at `25077be8211590e649bb37752aacaceaf88d3984`. **BI-056 CLOSED. BI-057 CLOSED.** BI-059 was CLOSED at `6a5eb9f` (Burst-35); confirmed unchanged at merge. Cycle-7 APPROVE verdict posted by human (`drbothen`, comment `5232703815`, 17:06:51Z) — no automation posted it. | Operator merged directly after self-posting the verdict comment — both steps required the human. BI-039/BI-060 controls correctly functioning; no automation posted. | phase-1d | 2026-08-09 | human/operator |
| D-174 | **FIRST AUTHORITATIVE 9-CHECKER BASELINE ON `develop` = `2ac2c3e`** — D-130 rules satisfied (on integration branch post-merge, not feature branch). **6 pass / 3 fail. Selftests 99/99.** Failing: `adr-consistency` — 9 violations (79 reason-code occurrences + 6 E-class occurrences), 134/134 files; `ec-injectivity` — 174 EC citations compared, 17 TV rows skipped (legitimately-EC-less: TV-BV013 + TV-S001..TV-S016), 42 divergent, 22 require adjudication; `holdout-boundary` — 1 violation, EC-151 at `prd.md:618`. Passing (6): canonical-facts (31 bindings/11 facts), counts (37 count checks), id-resolution (134 files), index-integrity (80 structural checks: BC 66, VP 26, HS 7), placeholders (0/134), title-sync (66 BC titles). **SUPERSEDES D-130 as authoritative mechanical baseline.** | D-130 standing lesson: the authoritative baseline is on the integration branch after merge; feature-branch baselines are provisional. | phase-1d | 2026-08-09 | orchestrator/operator |
| D-175 | **BI-057's repair visibly working in production.** Skip disclosure moved from 80 rows skipped with no soundness proof (pre-PR-#12) to **17 rows skipped, each named and justified** (TV-BV013 + TV-S001..TV-S016 — legitimately-EC-less rows). The row-granularity completeness hole identified by BI-057 is genuinely closed on `develop` = `2ac2c3e`. | Positive confirmation the repair achieved its structural objective. The skip disclosure is now an auditable assertion, not a silent omission. | phase-1d | 2026-08-09 | orchestrator |
| D-176 | **`malformed-fragment` phantom reason code DETECTED** at `test-vectors.md:432` (TV-197/EC-197) — adversary finding P7-S4-006. This is precisely the phantom code the product-owner REFUSED to invent during BI-053 (D-164). Had the code been invented, it would have added a second instance of a live-detected violation to the closed taxonomy. **Refusal to invent the code is vindicated by live detection.** This code is among the 9 violations reported by `check-adr-consistency` on `develop` = `2ac2c3e`. | D-164 positive precedent confirmed by live detection: the 13-code closed set is correct; a new code would have created a phantom that persists as a real-but-unclosable violation in every future run. | phase-1d | 2026-08-09 | orchestrator |
| D-177 | **Reason-code population moved 78→79** because BI-053's spec fix added a conforming occurrence of `anchor-not-found` (boundary-gap test vector for undecodable `%GG` fragments). Violations REMAINED at 9 — the added occurrence conforms. **CONSEQUENCE: GATE35 evidence artifacts on `develop` are STALE** — still record the figure as 78. Item-8 from PR #13 cycle-1 review: out of scope for PR #13 itself but COUPLED to item-5 (end-to-end demonstration); stale GATE35 evidence means the end-to-end match can never succeed using those artifacts (proven impossible per D-181). Stale evidence repair required before sweep PR closes. | 78→79 shift is an expected consequence of BI-053's conforming fix. Tracking the staleness explicitly prevents future misdiagnosis as a regression. | phase-1d | 2026-08-09 | orchestrator |
| D-178 | **PR #13 DELIVERED** — branch `fix/verifier-hardening-sweep-step0`, head `27688e36be6b21818c199671139f11a9b03a7032`. Base `develop` @ `2ac2c3e`. **4 files:** `scripts/verify-evidence-figures.py` (~900 lines, substantially rewritten); `scripts/tests/test-vef.py` (NEW selftest suite); `.github/workflows/ci.yml` (advisory step); `docs/demo-evidence/CHECKER-COMPLETENESS-GATE35/evidence-report.md` (NIT-E citation fix). **17/17 verifier selftests**; **99/99 nine-checker selftests**; item-0 context refusal works (`_VEF_TEST_N_AHEAD=0` → exit 2 with REFUSED diagnostic); hardcoded `"12"` and `CHECKER-COMPLETENESS-GATE35` removed from script body; `argparse` CLI with `--pr` / `--pr-desc` / `--evidence-dir`; PR resolution derived from current branch; artifact auto-discovery by `**Head SHA:**` stamp with REFUSED on 0 or 2+ candidates. 4/4 required CI green. `Spec lint` advisory red CORRECT per D-128. `MERGEABLE`. | Sweep step 0 deliverable (D-159/D-146). Verifier hardening before `verify-evidence-figures.py` becomes load-bearing (D-072 oracle-integrity principle). NIT-A and NIT-B from cycle-1 review are routed to the sweep PR scope, not PR #13. | phase-1d | 2026-08-09 | devops-engineer |
| D-179 | **PR #13 cycle-1 REQUEST_CHANGES.** Five blocking findings with reviewer's prescribed FIX ORDER: **(B-2 FIRST)** `headSha` regression reintroduced verbatim (`headSha` is not a `gh` field; correct name is `headRefOid`); suite reports 17/17 because every test stubs `gh` via `_VEF_TEST_*` env vars — a malformed `gh --json headSha` goes undetected on every real invocation; a suite green whether the dependency is broken OR absent is not a suite. **(B-4)** Hardcoded `39efec2` (commit from PR #12's branch) still required by rollback check; makes a PASS structurally impossible on any other PR. **(B-1)** Predicate polarity inverted in novel-spelling scan — WRONG figures pass clean. **(B-3)** `anchor_check()` proves ENTRY, not COMPARISON — `REQUIRED_CHECKS` registry can be satisfied without any live-vs-document comparison occurring. **(B-5)** CI advisory step reports `"conclusion":"skipped"` at head; wiring does not actually execute. Reviewer judgements: item-4 (explicit-flag bypass) — loud-mismatch ACCEPTABLE, not blocking. Item-5 (end-to-end matching-artifact case) — PROVEN IMPOSSIBLE (D-181). Item-8 (stale GATE35 evidence) — out of scope for PR #13 but COUPLED to item-5. | B-2 must be fixed first: until the suite can detect the `headSha` regression, no other fix can be trusted. Fix order is prescribed by the reviewer's dependency analysis. | phase-1d | 2026-08-09 | pr-reviewer |
| D-180 | **FOUR-MECHANISM FAIL-OPEN LINEAGE — standing lesson `[process-gap]`.** The same defect (a check reporting success while verifying nothing) has recurred FOUR times in `verify-evidence-figures.py` by NEW mechanisms: (1) Cycle 6 — bare `if match:` with no `else: fail`. (2) Cycle 7 — `re.search` validated only the FIRST restatement of each figure. (3) Sweep step 0 rounds 1-2 — all 16 selftests green while the tool was INERT: every test stubbed `gh` via `_VEF_TEST_*` env vars; a malformed `gh --json headSha` went undetected on every real invocation. A suite green whether the dependency is broken OR absent is not a suite. (4) Sweep step 0 round 3 — three NEW layers simultaneously: B-1 (predicate polarity), B-3 (registration semantics), B-5 (CI step ordering) PLUS mechanism 3 STILL OPEN (B-2). **Standing lesson: enumerating and patching known bypass shapes does not close this class. Only two things have actually worked: (a) a structural guarantee that makes the bad state UNREPRESENTABLE, and (b) a probe that does NOT share the code path it validates.** This is exactly why D-141 mandates INDEPENDENT-PROBE canary populations for all nine checkers. Carry this deliberately into the sweep. | Four mechanisms in one file's short life without a structural guarantee is the existence proof that enumeration is unbounded. The two defect-resistant designs (unrepresentable bad state + independent probe) are the default pattern, not a fallback. | phase-1d | 2026-08-09 | pr-reviewer + orchestrator |
| D-181 | **Item-5 (end-to-end matching-artifact case) PROVEN IMPOSSIBLE.** GATE35's existing artifacts record `adr-consistency` figure as **78**, but the live corpus on `develop` = `2ac2c3e` reports **79** (D-177 — BI-053's spec fix added one conforming occurrence). These artifacts can NEVER yield a PASS because `verify-evidence-figures.py` compares the stored figure against live checker output, and 78 ≠ 79. PR #13 has no artifact directory of its own. Therefore the end-to-end demonstration **MUST use PR #13's own artifacts** — PR #13 must create an artifact directory with correct figure 79 and demonstrate end-to-end exit 0 against it. Item-8 (stale GATE35 evidence) is COUPLED to item-5 for exactly this reason. | Not merely unproven but structurally proven impossible under current conditions. Correct resolution: PR #13 creates its own artifact directory with live-corpus figures; GATE35 evidence repair is a separate item. | phase-1d | 2026-08-09 | pr-reviewer + orchestrator |
| D-182 | **BI-060 EXTENDED with two new hook defects found by the PR #13 cycle-1 reviewer.** Previously recorded: (a) UNSATISFIABLE (demands `gh pr review`, impossible on self-authored PRs); (b) actively instructs agents toward self-approval. **Two additional defects:** (1) **FALSE NEGATIVE ON CYCLE-KEYED FILENAMES** — review file existed (40,786 bytes, first line `**VERDICT: REQUEST_CHANGES**`) BEFORE hook fired; hook appears to match literal `pr-review.md` while deliverable convention is `pr-review-cycleN.md` (GATE35 precedent: `pr-review-cycle7.md`); hook will misfire on EVERY cycle-keyed review; reviewer correctly did NOT rename the artifact to silence the hook — the fix belongs in the hook. (2) **MACHINE-UNREADABLE BLOCK STATE** — `block_reason=""` while `block_intent=true` and `exit_code=2`; blocking condition is not machine-readable. Operator ruling EXTENDED: do NOT modify the hook mid-run; record and route around by documented exception only (D-158). | Four total defects in BI-060 now. Each makes the hook less trustworthy as a gate. Reviewer behaviour was correct: did not rename artifact to silence hook. | phase-1d | 2026-08-09 | pr-reviewer + orchestrator |
| D-183 | **NIT-A and NIT-B from PR #13 cycle-1 review ROUTED to the sweep PR**, not dropped. NIT-A and NIT-B address issues in the `scripts/spec-lint/` subtree which was correctly FROZEN for PR #13 (scope confined to `scripts/verify-evidence-figures.py` per D-159). Routed to the gate-#42 nine-checker sweep PR where `scripts/spec-lint/` edits belong. NIT-A/NIT-B will be addressed in the sweep PR. Gate-#28 v3 verdict flow for PR #13 may proceed to cycle-2 once B-1..B-5 are fixed; NIT-A/NIT-B do not block cycle-2 but must land before the sweep closes. | Routing rather than dropping ensures the items are durably tracked and will land in the correct PR. | phase-1d | 2026-08-09 | orchestrator |
| D-184 | Burst-37 session wrap — durable RESUME SNAPSHOT D-184 committed to factory-artifacts, superseding D-172. Burst covers: PR #12 MERGED (2ac2c3e; BI-056/057 CLOSED); first authoritative 9-checker baseline (6 pass/3 fail; 79 reason-code; 174 ec-injectivity citations; EC-151 at prd.md:618); BI-057 repair confirmed working (17 named rows); malformed-fragment detected, refusal-to-invent vindicated (D-176); 78→79 shift + GATE35 evidence staleness (D-177); PR #13 OPEN at 27688e3 (17/17, item-0 refusal, argparse); PR #13 cycle-1 REQUEST_CHANGES B-2/B-4/B-1/B-3/B-5 with fix order; four-mechanism fail-open lesson codified [process-gap] D-180; item-5 proven impossible D-181; BI-060 extended two hook defects D-182; NIT-A/B routed to sweep D-183; D-173..D-184 (exhaustive). | Clean-boundary wrap. Pickup: fix PR #13 B-2 first (headSha→headRefOid + selftest that detects the regression), then B-4 + PR #13's own artifact directory + end-to-end exit 0, then B-1/B-3/B-5; cycle-2 via gate-#28 v3 (reviewer read-only + audit file; never merge without posted verdict). Same discipline as D-172, D-161. | phase-1d | 2026-08-09 | state-manager |
| D-185 | B-2 CLOSED. Field-contract made structural: `scripts/tests/test-vef.py` extracts the `gh --json` field list from the verifier source via `re.findall` with `assert len == 1` (refuses zero AND multiple matches), eliminating the duplicated literal; T18 added as an independent probe driving the verifier's REAL `gh` resolution path. Orchestrator verified the mutation experiment at THREE separate heads: mutating `"number,headRefOid"` → `"number,headSha"` drove the suite to 19/21, then 22/24, then 24/26, with T17 AND T18 both failing each time; restore returned green each time. Commit `09be233`. | Field-contract extracted from source; independent probe drives real `gh` path; mutation at three heads proves detection not accidental. | phase-1d | 2026-08-09 | devops-engineer |
| D-186 | B-4 CLOSED. Hardcoded `39efec2` (a commit on PR #12's branch) removed; the rollback requirement is now derived from `git rev-list develop..HEAD` as a SET difference rather than a cardinality check, so a right-count/wrong-membership list is caught. Bare `if count_m:` given an explicit `else: fail`. `check9` redesigned away from the circular "evidence-report Head SHA == live HEAD" (unsatisfiable because committing the value advances HEAD) to "captured-at SHA must be a member of `develop..HEAD`", non-circular because git history is append-only. Commit `3dc681b`; evidence `16b3513`, `f3bdf2f`. | Set-difference rather than cardinality; membership test replaces circular self-referential check. | phase-1d | 2026-08-09 | devops-engineer |
| D-187 | B-1 CLOSED WITH RESIDUAL. Novel-spelling scan re-keyed from the correct figure's VALUE to figure-adjacent CONTEXT, so a wrong figure in novel prose is now detected because it differs; uncovered context lines fail loudly. Probe cases B/C/D (wrong figures in novel prose) now caught; case A unregressed. **RESIDUAL FOUND BY ORCHESTRATOR DIRECT VERIFICATION:** the implementer reported "nothing surfaced" for S-7, having run ONLY the stubbed selftest suite and never the real corpus. Against the real corpus the fix produced TWO false positives on a historical-baseline table row in `pr-description.md` ("9 divergent, 5 adjudication" compared against live 42/22). Resolved via an explicit `EI_NOVEL_DECLARED` declaration with S-7 stale-declaration detection, enumerated loudly in output per D-039 (no suppression). Commits `8499a67`, `8593f4e`. | Real-corpus run mandatory before closure (L-63). Exemption bounded by stale-declaration detection rather than unbounded allowlist. | phase-1d | 2026-08-09 | devops-engineer/orchestrator |
| D-188 | B-3 CLOSED AS CLAIMED (later partially falsified — see D-191/B2-2). `anchor_check()` no longer writes to `checks_ran`; a new `record_comparison(key)` became the sole registration path, called where values are actually compared, so "registered but compared nothing" was intended to be structurally unrepresentable. PASS gate fails on any `REQUIRED_CHECKS` key never registered; orchestrator verified this enforcement path exists. Registry coverage extended 9 → 11 of ~14 comparison sites. Commit `8593f4e`. | Structural guarantee: registration and comparison collapsed into one call. Partially falsified in cycle-2 (D-191/B2-2). | phase-1d | 2026-08-09 | devops-engineer |
| D-189 | B-5 CLOSED AT THE STATUS LEVEL, SUBSEQUENTLY REFUTED AT THE SEMANTIC LEVEL (see D-192). `if: ${{ !cancelled() }}` plus a develop-ref guard added to the advisory CI step. Orchestrator confirmed via `gh run view 31340914559` that job `Spec lint` showed `Verify evidence figures (advisory)` = **success**, where it had previously been `skipped`. However this "success" was dishonest — see D-192(5a). Commit `04f5ec9`. | B-5 appeared closed on STATUS; D-192 proved the success was structurally dishonest (ran zero checks). | phase-1d | 2026-08-09 | devops-engineer/orchestrator |
| D-190 | **FIRST GENUINE END-TO-END EXIT 0.** `python3 scripts/verify-evidence-figures.py` with NO flags (auto-discovery) exited **0** printing `PASS — all figure checks match live output and git state`; verified by the orchestrator running it directly. Achieved with PR #13's OWN artifacts — precisely what D-181 proved impossible using GATE35's artifacts (GATE35 records reason-code 78; live is 79 per D-177). Packaging resolved the Head-SHA circularity: evidence commits on code branch FIRST, then `pr-description.md` authored on `factory-artifacts` naming the final head with a full 11-commit rollback list and matching count claim, then live PR body synced. Code head `7739995`; factory-artifacts `37d6275`. | Circularity resolved by ordering: code branch commits first, then factory-artifacts names the final head. First genuine end-to-end PASS. | phase-1d | 2026-08-09 | devops-engineer/orchestrator |
| D-191 | PR #13 cycle-2 verdict **REQUEST_CHANGES**, `covered_sha: 7739995e7e3f25c0af3dd9bcad535af2a7774fbb`. Five blocking findings B2-1..B2-5, five suggestions S2-1..S2-5, two nits N2-1/N2-2. Audit file at `.factory/code-delivery/VERIFIER-HARDENING-SWEEP-STEP0/pr-review-cycle2.md`. Reviewer's prescribed fix order: **B2-1 → B2-2 → B2-4+S2-1 → B2-3 → docs**, on the rationale that until CI can distinguish a working verifier from a broken one, no later green means anything. | Fix order driven by CI-honesty gate: B2-1 must be fixed before any other fix can be trusted. | phase-1d | 2026-08-09 | pr-reviewer |
| D-192 | **MECHANISM NUMBER FIVE FOUND — TWO OF THEM. The D-180 lineage extends from four rounds to five.** **5(a):** B-5's own fix converted an HONESTLY-INERT CI step (`skipped`) into a DISHONESTLY-GREEN one (`success`). The workflow has no `GH_TOKEN`, only `permissions: contents: read`, and `persist-credentials: false`, so `gh` cannot authenticate; the verifier exits 2, and the wrapper maps exit 2 to "REFUSED (context not applicable — not a failure)" and reports success. The step ran ZERO checks. The verifier conflates benign context, environment failure, and its OWN self-diagnosed bug ("possible bad JSON field name in verifier") all into exit 2 — so a broken verifier reports success in CI. Compounding: nothing in CI runs `test-vef.py`, so B-2's probe and B-5's wiring each depend on the other and neither is connected. **5(b):** the `EI_NOVEL_DECLARED` exemption is bounded by nothing — it matches `if d[0] in _site_ctx` over a ±40-character window, with no metric binding, no value binding, and no multiplicity bound, so it exempts ANY wrong figure near the fragment `110 of 190 TV rows`. Reviewer probe `P-D` passes with rc=0. The correct bounded pattern already exists 300 lines earlier in the same file (`PREV_LABEL`). | A fix that converts honest-inert to dishonestly-green is strictly worse. Every fix must be probed against the RELOCATED form of the defect it closes (L-65). | phase-1d | 2026-08-09 | pr-reviewer |
| D-193 | **ORCHESTRATOR VERIFICATION DEFECT [process-gap].** The orchestrator corroborated B-5 by reading the CI step's CONCLUSION (`success`) rather than its OUTPUT, and reported it as confirmation. The cycle-2 reviewer refuted it by pulling the actual step log, which showed `REFUSED — no open pull request found for current branch` and `(gh said: ... set the GH_TOKEN environment variable)`. The orchestrator independently re-verified the reviewer's refutation and confirmed it. A green step that executed zero checks is WORSE than a skipped step, because `skipped` is honest and `success` is not. This is the D-180 green-means-verified conflation applied to the orchestrator's own verification method, not an implementer's. | L-62 (load-bearing): a step's STATUS is not verification; only its OUTPUT is. This instance was committed by the orchestrator itself. | phase-1d | 2026-08-09 | orchestrator |
| D-194 | B2-4 confirmed by orchestrator direct execution. `evidence-report.md` declares `**Captured at SHA:** 3dc681b` while claiming AC-7 = 26/26, but at `3dc681b` `scripts/tests/test-vef.py` contained **21** test functions and `docs/demo-evidence/VERIFIER-HARDENING-SWEEP-STEP0/` contained **0** files. The captured-at claim is therefore FALSE, and `check9` accepts it because branch-membership is NECESSARY BUT NOT SUFFICIENT — any of the 11 branch commits satisfies it. AC-002 and AC-007 are also outside `STAMPED`, which is what allowed this to ship (S2-1). | L-64 (load-bearing): a provenance claim must be verified against what was TRUE AT that SHA, not merely that the SHA exists on the branch. | phase-1d | 2026-08-09 | orchestrator |
| D-195 | **gate-#28 v3 executed cleanly on its first run.** The reviewer remained read-only on the repository under review, wrote the cycle-keyed audit file `pr-review-cycle2.md`, did NOT attempt `gh pr review` (structurally impossible per BI-039/D-021/D-105), posted NO PR comment, and did NOT rename the artifact to silence the known `validate-pr-review-posted` false-negative — recording the false-negative in the report instead. ZERO auto-mode classifier denials occurred across the entire session's dispatches, versus four blocked runs (~1,027s, ~9.7% of session wall time) previously recorded under CI-063. The CI-063 mitigation — embedding operator authorization verbatim in each agent's INITIAL SPAWN PROMPT — held for every dispatch this session. | CI-063 zero-denial session is the first since the mitigation was adopted. Gate-#28 v3 clean execution confirmed. | phase-1d | 2026-08-09 | pr-reviewer/orchestrator |
| D-196 | Streak accounting CONFIRMED UNCHANGED at **0 of 3** clean passes, per operator ruling: spec fixes and checker PRs do not count toward the streak. No change. | Operator ruling (twice affirmed, informed). | phase-1d | 2026-08-09 | human/operator |
| D-197 | BI-060 second identity: standing human ruling is to CONTINUE THE CURRENT WAY (twice affirmed, informed). Carried FLAGGED for the pre-wave-1 checkpoint; **do not re-raise before then.** | Operator ruling. BI-060 second-identity question carried to pre-wave-1 checkpoint; do not re-raise earlier. | phase-1d | 2026-08-09 | human/operator |
| D-198 | Burst-38 session wrap — durable RESUME SNAPSHOT D-198 committed to factory-artifacts, superseding D-184. Clean-boundary wrap (D-112 discipline): all five cycle-1 blocking findings closed and verified D-185..D-189 (exhaustive), first genuine end-to-end exit 0 achieved (D-190), cycle-2 review complete with a durable audit file (D-191..D-194). No work left partially done. D-185..D-198 (exhaustive). | Same D-112 discipline. Pickup: fix PR #13 B2-1 first (CI honesty), then B2-2, B2-4+S2-1, B2-3, docs; cycle-3 via gate-#28 v3. NEVER merge on unposted verdict. | phase-1d | 2026-08-09 | state-manager |
| D-199 | B2-1 CLOSED with S2-3 and N2-2. Exit 2 split into: 0=PASS / 1=FAIL / 2=REFUSED-benign / 3=REFUSED-environment-or-auth-failure / 4=REFUSED-verifier-self-diagnosed-bug / 5=PARTIAL (runnable checks passed, one or more loudly skipped). CI wrapper FAILS on exit 3, 4, 5; docstring documents the scheme. PR number now derived from `github.event.pull_request.number` instead of unauthenticated `gh pr view`. Check 8 records a named entry in `checks_skipped` and the run exits 5 BEFORE the PASS print, so no code path leads from "check 8 skipped" to exit 0 — structurally non-passable. `test-vef.py` runs as its own CI step, connecting B-2's independent probe (S2-3 closed). N2-2 closed: a run with loud-skips exits 5, so "26/28 with 2 skips" can no longer read as a full pass. Suite 26→28 (T27 exit-3, T28 exit-5). Commits `0d730a5`, `00ec082`. | Operator-authorized B2-1 design ruling (D-203) applied. Exit-code scheme is exhaustive; CI wrapper structural guarantee on exit 5 is non-bypassable. | phase-1d | 2026-08-09 | devops-engineer/orchestrator |
| D-200 | PR MERGE-REF DEFECT: mechanism 5(a) was RELOCATED by its own first fix, caught by orchestrator direct verification. After the first B2-1 commit the advisory step still ran zero checks and still reported benign; the implementer attributed this to the frozen `pr-description.md` and called it correct. The orchestrator verified CI HEAD `900109420714435dd2a8fd5b8053b83b3d740c4a` and found it was `refs/pull/13/merge` — an ephemeral two-parent merge commit synthesized by GitHub (parents `2ac2c3e` develop, `00ec082` PR head), unreachable from the branch and absent from every committed artifact. Therefore SHA-based auto-discovery was structurally incapable of EVER matching on a `pull_request` event — permanently, not just while frozen — so the step would stay green having compared nothing on every future run. Closed by checking out `${{ github.event.pull_request.head.sha \|\| github.sha }}` with `fetch-depth: 0`, plus a structural guarantee in the wrapper: any exit 2 on a `pull_request` event FAILS, keyed on `GITHUB_EVENT_NAME` rather than an enumeration of REFUSED reasons, so new REFUSED paths fail automatically. Orchestrator-verified from the CI log: checkout `ref: f4c43e68…` (single parent, not a merge SHA), verifier reached artifact discovery, wrapper emitted `verify-evidence-figures: FAIL — REFUSED on pull_request event` and `##[error]Process completed with exit code 1`. Gate now LIVE. Commit `f4c43e6`. | Three distinct forms of status-is-not-verification in one session: step-conclusion-vs-zero-check-output (D-193), merge-ref masking (here), `continue-on-error` masking (D-201). | phase-1d | 2026-08-09 | orchestrator |
| D-201 | L-62 recurred in a THIRD form, again against the orchestrator's own verification method. With `continue-on-error: true`, GitHub reports the advisory step's CONCLUSION as `success` even when it exits 1; the orchestrator saw `success` and had to read the step log to get the true verdict. For any `continue-on-error` step the conclusion field is structurally incapable of signalling failure — only the log is authoritative. Three distinct forms of status-is-not-verification in one session: step-conclusion-vs-zero-check-output (D-193), merge-ref masking (D-200), `continue-on-error` masking (here). | Standing rule: any claim about a `continue-on-error` step passing or failing correctly MUST quote the log. L-66 codified. | phase-1d | 2026-08-09 | orchestrator |
| D-202 | B2-1 RESIDUAL, OPEN VERIFICATION ITEM. The CI gate is proven LIVE (fails when it cannot compare) but has NOT been observed reaching actual figure comparisons in CI, because `pr-description.md` is deliberately stale until final packaging. Cycle-3 MUST verify from the CI LOG that, after packaging refreshes the artifact, the advisory step shows real comparison output ("N occurrence(s) compared/validated") and not a REFUSED. Until observed in the log, "CI verifies evidence figures" remains UNPROVEN. | Same D-082 executed-predicate constraint. Verification requires observing log output, not just status. | phase-1d | 2026-08-09 | orchestrator |
| D-203 | Operator ruling on B2-1 design: unprivileged honest reduced set chosen over a CI token grant. `GH_TOKEN` NOT added, `permissions` NOT widened; the token grant remains separately gated, to be revisited before the nine-checker sweep lands. Check 8 therefore cannot run in CI by design and must skip loudly. | Operator explicitly DECLINED the `GH_TOKEN`/`permissions` grant; it was NOT escalated. Do NOT add `GH_TOKEN` or widen `permissions` without a fresh operator ruling. | phase-1d | 2026-08-09 | human/operator |
| D-204 | Burst-39 session wrap; durable RESUME SNAPSHOT D-204 committed, superseding D-198. Clean-boundary wrap on context grounds (D-112 discipline): B2-1 fully closed and orchestrator-verified, all remaining work precisely specified, nothing partially done, both worktrees clean and synced. D-199..D-204 (exhaustive). | Same D-112 discipline. Pickup: B2-2 (eliminate register-without-comparing at check2a-e-cli-001). | phase-1d | 2026-08-09 | state-manager |
| D-205 | NEW STANDING DIRECTIVE — BUILD-SUFFICIENCY RULING (human, journaled). Enumeration serves construction: the spec must be correct enough to BUILD, not defect-free. Effective immediately for the gate-#42 sweep's remediation round and ALL subsequent adversary passes. **MUST-FIX** = defects that would corrupt implementation: wrong BCs, false-green VP attribution, EC mis-anchoring, holdout integrity. **ADJUDICATE-AND-RECORD-NOT-FIX** = historical, cosmetic, gloss, and index findings — documented-accepted with an explicit reason; honest checkers MAY continue reporting them without blocking. **Pre-defined deal-breakers** (ONLY these stop the run; operator escalates to human): (a) pass 8 finds a NEW content defect class; (b) the new-CRITICAL rate is not decaying; (c) a domain-model-invalidating defect. This supersedes any prior implicit "fix every finding" posture. Recorded as a standing constraint. | Operator-journaled human ruling. | phase-1d | 2026-08-09 | human/operator |
| D-206 | CLEAN-PASS CRITERIA SURFACED (operator request, pre-pass-8) — and a STRUCTURAL CONFLICT with D-205 identified. **PG-012 in this project is a severity-REPORTING policy** (RANGES, CRITICAL/HIGH/MEDIUM/LOW vocabulary per D-095/D-115/D-154). The machine-enforced clean-pass definition lives in `hooks/convergence-tracker.sh`: a pass is CLEAN iff `| CRITICAL | 0 |` AND `| HIGH | 0 |`. `CONVERGENCE_REACHED` requires novelty score ≤ 0.15 + 0 CRITICAL + 0 HIGH this pass + at least 3 consecutive sibling pass files each with 0 CRITICAL and 0 HIGH. **CONFLICT:** the hook counts by SEVERITY ONLY — no materiality dimension, no NEW-vs-REPEAT. A HIGH-severity documented-accepted finding still BREAKS the streak; accepted findings that honest checkers keep reporting would hold the streak at 0 PERMANENTLY. **Recommended alignment (no engine change):** adversary's counted severity table tabulates ONLY un-adjudicated build-impact (MUST-FIX) findings; documented-accepted findings in a separate explicitly NON-COUNTED section the hook does not parse; NEW-vs-REPEAT maps onto novelty score (≤ 0.15). **Secondary latent conflict:** PG-012 mandates RANGES while hook parses single integer; ranges are safe only at 0. **OPERATOR RULING REQUIRED before pass 8 dispatches.** | Structural conflict surfaced before pass 8; D-205 alignment requires operator ratification. | phase-1d | 2026-08-09 | orchestrator |
| D-207 | B2-2 CLOSED — the register-without-comparing class ELIMINATED, not relocated. `record_comparison(key)` became `record_comparison(key, *, doc_value: object)` — keyword-only with NO default, so omitting raises `TypeError` at call site and `doc_value=None` raises `AssertionError` inside. `check2a-e-cli-001` rewritten as positive assertion so registration is reachable only when the subject token is present. The cycle-2 production-path attack reproduced (verifier exited 0 BEFORE) and caught (exits 1 AFTER, naming both `traceability/e-cli-001-in-pr` and `required-check/check2a-e-cli-001`). Per-site sweep of all 11 required checks found only `check2a` defective; other 10 already passed a non-None `doc_value`. Suite 28→29. **Honest residual disclosed:** `doc_value` not SEMANTICALLY validated; accidental omission unrepresentable, deliberate circumvention is not. Orchestrator-verified: signature keyword-only with no default; suite 29/29; tree clean; local == remote. Commit `0593be3`. | Elimination (not relocation) closes the class structurally. | phase-1d | 2026-08-09 | devops-engineer |
| D-208 | NEW GENUINE FINDING surfaced by the B2-2 fix: `traceability/e-cli-001-in-pr`. The real `pr-description.md` does not acknowledge the live-detected `E-CLI-001` E-class code. This is a real traceability gap, not a false positive of the new check. Under D-205 it is **MUST-FIX** (detected E-class code unacknowledged) and MUST be closed in the final packaging step. | Must-fix per D-205; close in final packaging. | phase-1d | 2026-08-09 | orchestrator |
| D-209 | Operator RATIFIED the orchestrator's discard-and-rerun of the stalled Burst-39 partial write, in which an API-killed state-manager had written only STATE.md frontmatter falsely claiming "Burst-39 COMPLETE" while none of D-199..D-204 (exhaustive), the BI updates, the lessons, or the snapshot existed. Recorded as correct handling of the false-green state class. | Discard-and-rerun was correct. False-green state from partial write must be discarded, not patched. | phase-1d | 2026-08-09 | human/operator |
| D-210 | Burst-40 session wrap; durable RESUME SNAPSHOT D-210 committed, superseding D-204. Clean-boundary wrap on context grounds (D-112 discipline). | Same D-112 discipline. Pickup: B2-4+S2-1 → B2-3 → B2-5 → final packaging (incl. D-208 E-CLI-001 gap) → cycle-3. | phase-1d | 2026-08-09 | state-manager |
| D-211 | D-206 PARTITION ALIGNMENT RATIFIED as a recorded ruling, with TWO BINDING GUARDS. The report-side partition (counted severity table = un-adjudicated MUST-FIX build-impact findings only; documented-accepted findings in a separate NON-COUNTED section the hook does not parse) is ratified. NO engine or hook change — partition is report-side and openly recorded (D-206 / L-69 / this ratification). **Guard 1 — ADJUDICATION LEDGER:** every finding moved into the documented-accepted section MUST carry its reason AND adjudicator inline; every pass report MUST render BOTH sections — counted must-fix table AND the non-counted accepted section — so nothing becomes invisible; moving a finding between sections is ITSELF a recorded event. **Guard 2 — NEW-CLASS ASSERTION:** the NEW dimension does NOT rest on the novelty score alone (self-reported — acknowledged measurement gap); each pass report MUST include an explicit NEW-CLASS assertion listing findings that do not map to any known class; the operator evaluates the D-205 deal-breaker test from THAT LIST, not from the novelty number. PG-012 application amended: severity table MUST use EXACT INTEGERS; ranges are permitted in PROSE ONLY. These requirements bind gate-#42 sweep remediation round and every subsequent adversary pass and MUST be embedded in every adversary dispatch prompt. | Ratified. Two guards bind all future adversary passes. | phase-1d | 2026-08-10 | human/operator |
| D-212 | NEW SCOPE ITEM: fix the L-66 `continue-on-error: true` conclusion-masking so the D-202 CI-log check is meaningful. Because `continue-on-error: true` makes GitHub report the advisory step's CONCLUSION as `success` regardless of exit code, log-based verification is the only signal and is easy to skip. Fix must land before or together with cycle-3. Constraint: D-203 `GH_TOKEN` must NOT be added and workflow `permissions` must NOT be widened without a fresh operator ruling — fix must respect that. | Must land before or with cycle-3. D-203 no-token-grant constraint preserved. | phase-1d | 2026-08-10 | human/operator |
| D-213 | Burst-41 session wrap; durable RESUME SNAPSHOT D-213 committed, superseding D-210. Clean-boundary wrap taken on context grounds (D-112 discipline) immediately after recording the D-211 ratification, so that the governing ruling could not be lost. No fix work was started this turn; B2-4 + S2-1 remains the pickup point. | Same D-112 discipline. Pickup: B2-4+S2-1 → B2-3 → B2-5 → final packaging (incl. D-208 E-CLI-001 gap) → D-212 (fix continue-on-error masking) → cycle-3. | phase-1d | 2026-08-10 | state-manager |
| D-214 | OPERATOR DIRECTIVE (human, this session): GOOD ENOUGH is the standard, explicitly including the verifier itself. (1) Cycle-3 findings on PR #13 that do not create false-green risk are ADJUDICATE-AND-RECORD, not fix — no cycle-4 for polish. (2) Nine-checker sweep scoped to ledger + anti-tautology + independent-probe populations per D-141 — not exhaustive beautification. (3) Same bar for post-sweep remediation: must-fix only what corrupts implementation. Goal: pass 8 against honest oracles → gate → BUILDING. This AMENDS D-205 application. | Operator-journaled human ruling. | phase-1d | 2026-08-10 | human/operator |
| D-215 | B2-4 + S2-1 + S2-4 CLOSED plus two orchestrator-found residuals. R1: `_expected_stamp_sha7 is not None` silently unenforceable → added `evidence-report/stamp-sha-unavailable` fail when stamp unavailable in production; "graceful degradation" comment replaced with true invariant. R2: T30 used mock + T31 skipped equality path → added T32 with real check7-derived `_verified_stamp_sha` and disagreeing report SHA, proven non-tautological by neutralizing the assertion. `check9` now requires `**Captured at SHA:**` to EQUAL the check7-verified stamp (on-branch is necessary but not sufficient). AC-002 re-captured as full 226-line run. AC-002/AC-007 added to STAMPED. `check7` given real coverage via throwaway git repo (`_VEF_TEST_STAMP_REPO`), positive + negative. **Known disclosed gap (ADJUDICATE-AND-RECORD per D-214):** R1 fail path unreachable in tests because `_TEST_MODE` always true; rests on code inspection. Not fixed per D-039 risk of introducing suppression-shaped exemption. | Elimination + honest gap disclosure. | phase-1d | 2026-08-10 | devops-engineer |
| D-216 | B2-3 CLOSED via reviewer OPTION 1 (preferred). Implemented `_strip_prev_col()`: cell-aware historical-column filter locating `Previous (post-gate34)` column header in markdown tables and blanking only that column's cells in data rows; applied to both `pr-description.md` and `evidence-report.md`. The novel-spelling declaration channel DELETED ENTIRELY — both `ADR_NOVEL_DECLARED` and `EI_NOVEL_DECLARED` and all readers, including the `d[0] in _site_ctx` ±40-char proximity binding that WAS the B2-3 finding; an uncovered novel-spelling site now fails unconditionally. Two structural no-op guards added: `pr-baseline/prev-column-header` and `pr-baseline/prev-column-rows`. Fixed latent bug: `_ev_offset` misalignment (ev section starts at `len(pr_no_prev)+1`, not `len(pr)+1`). Suite 29→48 (T33-T48 cover six probe shapes P-A/P-A2/P-B/P-C/P-C2/P-D, six L-65 relocated variants, filter guards, and unconditional-fail after channel deletion). | Channel deletion closes the class structurally, not by data deletion. | phase-1d | 2026-08-10 | devops-engineer |
| D-217 | D-212 CLOSED. `verify-evidence-figures` job moved out of `Spec lint` into its own top-level job with no `continue-on-error` at step or job level (job-level would have reintroduced L-66 one level up). Guard tightened to `if: ${{ !cancelled() && github.event_name == 'pull_request' }}` so push events skip (not red). D-203 preserved — no `GH_TOKEN`, `permissions: contents: read` unchanged. Second masking path fixed: develop-unresolvable was `exit 0` → now `exit 5` LOUD-SKIP. Orchestrator-verified: push event → job `skipped`; pull_request event → job conclusion honestly `failure`. | Structural fix; no token grant; D-203 preserved. | phase-1d | 2026-08-10 | devops-engineer |
| D-218 | B2-5 and D-208 CLOSED; final packaging complete. B2-5: Risk Assessment no longer claims "no CI workflow changes"; `.github/workflows/ci.yml` added to Systems affected. D-208: `pr-description.md` now acknowledges live-detected `E-CLI-001` (BC-2.11.004.md line 61, POLICY 19 violation, tracked not fixed). Stale figures refreshed to 48. Branch pushed; live PR body synced. Local end-to-end exit 0 verified by orchestrator with real comparisons: adr-consistency 8 compared; e-class-ledger triple 4 compared; ec-injectivity citations-compared 14, divergent 15, adjudication 14 validated. | Final packaging closes all remaining must-fix items. | phase-1d | 2026-08-10 | devops-engineer |
| D-219 | D-202 CLOSED. FIRST OBSERVED CI-LOG CONFIRMATION of real figure comparisons. CI run `31365193619` (event `pull_request`, head `eae146a`) log body shows: `adr-consistency figures: 8 occurrence(s) compared`; `e-class-ledger triple: 4 occurrence(s) compared`; `ec-injectivity citations-compared: 14 occurrence(s) validated`; `divergent: 15 validated`; `adjudication: 14 validated` — identical to the local run. Satisfies the operator requirement that figure comparison be confirmed from the CI LOG, not the step status (L-62/L-66/L-68). Job conclusion is `failure` with `verify-evidence-figures: PARTIAL — some checks skipped, not a full pass (exit 5)` because `check8` loud-skips without a token per D-203. | D-082 executed-predicate constraint satisfied via CI LOG observation. | phase-1d | 2026-08-10 | orchestrator |
| D-220 | OPEN OPERATOR DECISION: CI-green is unreachable for the `verify-evidence-figures` job under D-203. `check8` requires `gh` auth; without token it loud-skips → exit 5 (PARTIAL) → job red on every PR. Treating the skip as a pass would be fail-open (the exact class this sweep exists to eliminate), so exit 5 is correct. Operator must choose: (a) grant a CI token, reversing D-203, so the job can reach green; or (b) accept a permanently-red advisory job whose LOG is the authoritative signal. Not papered over, not silently accepted. Recorded for operator ruling. | Open operator decision — not resolved by state-manager. | phase-1d | 2026-08-10 | orchestrator |
| D-221 | PROCESS-GAP: the orchestrator over-fixed before the D-214 directive landed. A third sub-round was dispatched on B2-3 to delete the dead declaration channel when both lists were already empty — no live false-green risk, latent-only. Under D-214 that is ADJUDICATE-AND-RECORD, not fix. The work landed and is sound, but the judgement was wrong on cost. Codification: apply the D-214 false-green test BEFORE dispatching any additional sub-round, and prefer recording latent-only mechanism weaknesses. | Process-gap; work is sound. | phase-1d | 2026-08-10 | orchestrator |
| D-222 | Burst-43 session wrap; durable RESUME SNAPSHOT D-222 committed, superseding D-213. Clean-boundary wrap: sweep step 0 complete (all cycle-2 blocking items closed + D-202/D-208/D-212 closed), D-214..D-222 (exhaustive) recorded, RESUME SNAPSHOT D-222 drafted. Pickup: cycle-3 review of PR #13 at head `eae146a` via gate-#28 v3. | Same D-112 discipline. | phase-1d | 2026-08-10 | state-manager |
