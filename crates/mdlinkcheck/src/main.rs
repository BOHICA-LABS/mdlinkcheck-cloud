//! `mdlinkcheck` binary entry point.
//!
//! Full CLI implementation (clap arg parsing, pipeline orchestration) is in S-1.02.
//! This stub satisfies the workspace compilation requirement for S-1.01.
//!
//! `scanner` is now exposed via the library crate (`src/lib.rs`), which is the
//! conventional Rust pattern for making binary-crate modules reachable from
//! integration tests in `tests/`.

fn main() {
    todo!()
}
