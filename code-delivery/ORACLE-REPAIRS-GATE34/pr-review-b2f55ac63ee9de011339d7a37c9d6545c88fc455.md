# PR Review — Cycle 5 (stale-verdict re-assessment)

**PR:** #11 — `fix/oracle-repairs-gate34`
**Previously covered SHA:** `7e4a8f014a2e09e59757d1e26dbc4ce6073e1770` (cycle 4, APPROVE)
**Now covered SHA:** `b2f55ac63ee9de011339d7a37c9d6545c88fc455`
**Scope of this cycle:** incremental diff `7e4a8f0..b2f55ac` (1 commit), plus re-run of the test gate.

This is a delta review. The cycle 4 APPROVE stands for everything at or below
`7e4a8f0`; I re-examined only what changed since, and re-ran the full selftest gate to
confirm the previous evidence still holds at the new head.

---

## Incremental Diff

```
docs/demo-evidence/ORACLE-REPAIRS-GATE34/evidence-report.md | 6 +++---
1 file changed, 3 insertions(+), 3 deletions(-)
```

Three lines changed, all of them `21` → `22`:

| Line | Change |
|------|--------|
| 16 | AC-3 coverage-summary row: "21 new oracle-repair selftests" → "22 new" |
| 63 | "New selftests added by this PR (21 total…)" → "(22 total…)" |
| 90 | "**Result: PASS — all 21 new tests pass…**" → "all 22 new tests pass" |

---

## What I Verified

### 1. Diff confinement — CONFIRMED

The commit touches exactly one file: `docs/demo-evidence/ORACLE-REPAIRS-GATE34/evidence-report.md`.
No checker code, no selftest orchestrator, no fixtures, no primitives module. Independently
confirmed against the whole-PR range as well: `git diff --name-only f8ee4eb..b2f55ac -- .factory/specs/`
returns 0 files — the frozen spec perimeter is intact.

### 2. Arithmetic — CONFIRMED, and verified from source rather than prose

I did not take the stated baseline on faith. I read `EXPECTED_TEST_COUNT` out of the
selftest orchestrator on both sides:

| Revision | `scripts/spec-lint/selftest/run-selftests.sh:27` |
|----------|--------------------------------------------------|
| `f8ee4eb` (develop tip, = merge base) | `EXPECTED_TEST_COUNT=69` |
| `b2f55ac` (PR head) | `EXPECTED_TEST_COUNT=91` |

91 − 69 = **22**. The correction is arithmetically right, the pre-branch baseline really was
69 (not 75), and 69 is the correct subtrahend because `f8ee4eb` is also the merge base
(`git merge-base develop b2f55ac` → `f8ee4eb`), so no develop-side selftests landed in
parallel that would need accounting for.

I also cross-checked the count a second, independent way: the AC-3 enumeration
(`G5, P14-15/16/17, EI-1/2/3/4/5/6, 7b/7c/7d, 5b/5c/5d/5e/5f/5g/5h/5i/5j`) expands to
1 + 3 + 6 + 3 + 9 = 22, and the detail table at lines 67–88 contains exactly 22 rows, one per
enumerated test. Three independent derivations agree on 22. The old value of 21 was wrong.

### 3. No code or corpus touched — CONFIRMED

Documentation-only. Nothing in this commit can change checker behaviour, and nothing in it
touches `.factory/specs/`.

### 4. Test gate re-run at new head — PASS

Ran `bash scripts/spec-lint/selftest/run-selftests.sh` in the worktree at `b2f55ac`
(working tree clean, HEAD = `b2f55ac`):

```
Pre-flight guard passed: 15 checkers/generators support SPEC_LINT_REPO_OVERRIDE
Pre-flight guard passed: 15 checkers/generators scanned, 0 proven scope reductions, 0 unproven
10/10 primitive tests passed
Pre-flight guard passed: 15 files checked, 0 raw .splitlines() uses
Selftest passed: 91/91 negative tests verified (each proved clean-pass + defect-fail)
```

Exit code **0**. 101 individual `PASS` lines observed. 91/91 still green at the new head, as
claimed.

---

## Findings

### WARNING-11 (carries forward) — the 21→22 correction is incomplete: section heading at line 59 still says "21 new"

| Field | Value |
|-------|-------|
| Severity | **warning** (non-blocking) |
| Category | description / coherence |
| File | `docs/demo-evidence/ORACLE-REPAIRS-GATE34/evidence-report.md:59` |

The commit fixed three of the four occurrences of the stale count. The AC-3 section heading
was missed:

```markdown
## AC-3: Oracle-Repair Selftests (21 new, all PASS)
```

Two lines further down the same section now reads "New selftests added by this PR (22 total…)",
and the section closes with "all 22 new tests pass". So the document now contradicts itself
inside a single section — heading says 21, body and table say 22. That is arguably a worse
state than before the commit, when at least all four occurrences agreed on the same wrong
number.

This is a one-character edit. Suggested fix:

```markdown
## AC-3: Oracle-Repair Selftests (22 new, all PASS)
```

**Why this is a warning and not blocking:** the originating finding (WARNING-10 in cycle 4)
was itself classified non-blocking. A partially-applied fix to a non-blocking finding cannot
escalate above the severity of the finding it addresses, and the residual has zero effect on
the checkers, the selftest gate, the baseline, or CI. I am not going to hold a merge on a
stale integer in an evidence-report heading. But it is a real inaccuracy in the demo evidence
and it should be corrected — ideally in the same commit as WARNING-12 below.

### WARNING-12 (pre-existing, now more visible) — PR description carries three mutually inconsistent counts for the same quantity

| Field | Value |
|-------|-------|
| Severity | **warning** (non-blocking) |
| Category | description |
| File | PR #11 body |

The PR body states the new-selftest count three times, with three different values:

| Body line | Text | Value |
|-----------|------|-------|
| 148 | Coverage Summary: "New selftests this PR \| **22 new** (G5, P14-15/16/17, EI-1/2/3/4/5/6, 7b/7c/7d, 5b…5j)" | 22 ✅ |
| 220 | Demo Evidence table, AC-3: "13 new oracle-repair selftests all pass (G5, P14-15/16/17, EI-1/2/3, 7b/7c/7d, 5b/5c/5d)" | 13 ❌ |
| 320 | Performance Impact: "13 additional tests, each running clean+defect passes" | 13 ❌ |

Lines 220 and 320 are stale snapshots from an earlier cycle — line 220 also carries a
truncated enumeration (`EI-1/2/3` and `5b/5c/5d`) that predates EI-4/5/6 and 5e–5j. Now that
the evidence report has been corrected to 22, the PR body is the remaining inconsistent
artifact, and a human reader comparing the PR body's Demo Evidence table against the linked
evidence report will see 13 vs 22 and reasonably wonder which is true.

Suggested fix: update line 220 to mirror the corrected AC-3 row (22 new, full enumeration),
and line 320 to "22 additional tests". Note that the ~55s selftest runtime figure was measured
with the full suite, so only the count needs changing, not the timing.

### NIT-13 — commit message claims a change that is not in the diff

| Field | Value |
|-------|-------|
| Severity | **nit** |
| Category | commit quality |
| Commit | `b2f55ac` |

Subject line: `docs(demo-evidence): fix test count 21→22 and baseline 75→69 in evidence report`.

The "baseline 75→69 in evidence report" half of that claim is not in the diff. I searched both
revisions of the evidence report for `75` and for `69` and neither string appears in either
`7e4a8f0` or `b2f55ac`. The evidence report never contained a `75` to correct.

The 69 figure is stated correctly in the *PR body* ("91/91 (up from 69 pre-gate34)"), so the
substance is fine and the ground-truth reasoning in the commit body is accurate — it is the
subject line that over-claims scope. Harmless here, but a commit message that describes edits
it did not make degrades `git log` as an audit trail, which matters more than usual on a PR
whose headline deliverable is the honesty of its own baseline numbers.

Suggested subject for any follow-up amend: `docs(demo-evidence): fix new-selftest count 21→22 (91−69) in evidence report`.

### NIT-14 — evidence report header pins a now-stale head SHA

| Field | Value |
|-------|-------|
| Severity | **nit** |
| Category | description |
| File | `docs/demo-evidence/ORACLE-REPAIRS-GATE34/evidence-report.md:5` |

`**Recorded:** 2026-08-08 (refreshed at head SHA 4722ca16fa512b5e060ca5034adba0014d99a63b)`

Head is now `b2f55ac`, two commits past `4722ca1`. The recorded evidence is still valid — I
re-ran the suite at `b2f55ac` and got the identical 91/91 result, so the numbers have not gone
stale even though the SHA reference has. Worth refreshing if the file is touched again for
WARNING-11 anyway; not worth a commit on its own.

---

## Checklist

| # | Item | Result |
|---|------|--------|
| 1 | Diff coherence | PASS — single-purpose docs commit, confined to the one evidence file |
| 2 | Description accuracy | WARNING-12, NIT-13 — PR body carries 13 vs 22; commit subject over-claims |
| 3 | Test coverage | PASS — 91/91 re-verified at new head, exit 0; docs-only commit needs no new tests |
| 4 | Demo evidence | WARNING-11 — evidence report present and substantively accurate, but self-contradictory at line 59 |
| 5 | Commit quality | NIT-13 — conventional format and correct scope, subject over-claims one edit |
| 6 | Diff size | PASS — 6 lines |
| 7 | Missing changes | WARNING-11 — the intended correction was applied 3 of 4 times |
| 8 | Dependency status | PASS — merge base is develop tip `f8ee4eb`; no upstream PRs blocking |

## Constraints Honoured

- No fix suggested anywhere under `.factory/specs/`. All four findings sit in
  `docs/demo-evidence/` or in PR/commit metadata.
- Red spec-lint CI treated as the intended deliverable, not a defect. Not raised as a finding.
- Posted as a PR comment, not `gh pr review` (self-review makes approve/request-changes
  unavailable).

---

## Assessment

The one commit added since the cycle 4 APPROVE is a six-line documentation correction that
touches no executable code and no spec corpus. Its arithmetic is right, and I confirmed that
independently from the orchestrator source rather than trusting the prose: develop was at 69,
the branch lands at 91, so 22 is the correct count and 21 was wrong. The full gate still
passes 91/91 with exit 0 at the new head, so the cycle 4 evidence remains valid.

The commit did not finish its own job — one of four occurrences of the stale count survives, in
the AC-3 section heading, and the PR body still reports 13 in two places. Both are cosmetic
count inconsistencies in documentation, and both descend from a finding that was itself
classified non-blocking, so neither justifies holding the merge. They should be swept up in a
single follow-up docs commit before merge if one is convenient, but they do not invalidate the
deliverable: the four repaired oracles, the 91/91 non-vacuous selftest suite, and the true
mechanical baseline are all unchanged and all still verified.

Cycle 4's APPROVE carries forward to the new head.

VERDICT: APPROVE

covered_sha: b2f55ac63ee9de011339d7a37c9d6545c88fc455
